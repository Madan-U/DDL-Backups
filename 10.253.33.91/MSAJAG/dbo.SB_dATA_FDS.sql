-- Object: PROCEDURE dbo.SB_dATA_FDS
-- Server: 10.253.33.91 | DB: MSAJAG
--------------------------------------------------

--SB_dATA_FDS 'IFTE'

CREATE PROCEDURE [dbo].[SB_dATA_FDS]
(
@SUB_BROKER VARCHAR(20)
)
AS 
BEGIN

--SELECT CL_CODE,LONG_NAME,BRANCH_CD,SUB_BROKER,REGION,					---- COMMENTED ON 03 FEB 2026 SRE-43535
--(CASE WHEN B2C='Y'THEN 'B2C'ELSE 'B2B'END )AS B2B_B2C,COMB_LAST_DATE AS LAST_TRADE_dATE
-- FROM INTRANET.RISK.DBO.CLIENT_DETAILS WHERE SUB_BROKER=@SUB_BROKER
-- ORDER BY CL_cODE

 SELECT CL_CODE,LONG_NAME,BRANCH_CD,SUB_BROKER,REGION,					---- ADDED ON 03 FEB 2026 SRE-43535
(CASE WHEN B2C='Y'THEN 'B2C'ELSE 'B2B'END )AS B2B_B2C, CAST(COMB_LAST_DATE AS VARCHAR (11)) AS LAST_TRADE_DATE ,
CAST(FIRST_ACTIVE_DATE AS VARCHAR (11)) AS FIRST_ACTIVE_DATE,
CAST(LAST_INACTIVE_DATE AS VARCHAR (11)) AS LAST_INACTIVE_DATE,
(CASE WHEN LAST_INACTIVE_DATE LIKE '%2049%'THEN 'ACTIVE' ELSE 'INACTIVE' END )AS STATUS
 FROM INTRANET.RISK.DBO.CLIENT_DETAILS WITH (NOLOCK) WHERE SUB_BROKER=@SUB_BROKER
 ORDER BY CL_CODE

 END

GO
