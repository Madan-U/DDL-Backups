-- Object: PROCEDURE dbo.PROC_COMBINE_MARGIN_REPORTING_PEAK_NEW_FEB10
-- Server: 10.253.33.91 | DB: MSAJAG
--------------------------------------------------




--ALTERED THIS SP UNDER SRE-34462 
CREATE PROCEDURE [dbo].[PROC_COMBINE_MARGIN_REPORTING_PEAK_NEW_FEB10]   
( 
	@MARGINDATE VARCHAR(11),  
	@COMBINEFLAG INT = 1,  
	@FORREPORTING VARCHAR(1)='Y',  
	@CHECKFLAG INT = 0  ,
	@PEAK CHAR(1)='0'
)      
 WITH RECOMPILE 
AS      

  SET @PEAK ='1'

  /*
  IF (@MARGINDATE > 'FEB 15 2023')
  BEGIN
  SET @PEAK ='0'
  END
  ELSE
  BEGIn
  SET @PEAK ='1'
  END */

-- EXEC PROC_COMBINE_MARGIN_REPORTING_PEAK_NEW 'FEB  3 2022', 1, 'Y', 0  
  
  
--DUE TO COMBINE_REPORTING LIVE   
--SET @COMBINEFLAG = 0  
  
  
  
SET NOCOUNT ON   

DECLARE @PREVDATE VARCHAR(11)
DECLARE @EPNDATE VARCHAR(11)

DECLARE @EXCHANGE_NEW VARCHAR(3)  
  
DECLARE @SEGMENT_NEW VARCHAR(10)  
  
DECLARE @SHAREDB VARCHAR(35)  
  
DECLARE @SHARESERVER VARCHAR(35)  
  
DECLARE @ACCOUNTDB VARCHAR(35)  
  
DECLARE @ACCOUNTSERVER VARCHAR(35)  
  
  
  
DECLARE @EXCHANGEWISE_CURSOR CURSOR   
  
DECLARE @STRSQL VARCHAR(8000)   
  
DECLARE @MARGINDATENEW VARCHAR(11)  
  
DECLARE @MARGINDATECURSOR CURSOR  
  
DECLARE @MRGCUR CURSOR  
  
  
  
DECLARE @CNT INT  
  
DECLARE @DAY INT  
  
DECLARE @PARTY_CODE VARCHAR(10)  
  
DECLARE @SHRT_AMT NUMERIC(18,4)  
  
DECLARE @MARG_AMT NUMERIC(18,4)  
  
  
  
DECLARE @EXCHANGE_PRIORITY INT  
  

DECLARE 
@VARKEY 	VARCHAR(8),
@TRADEDATE	VARCHAR(11),
@PREVTRADEDATE VARCHAR(11)

SET @TRADEDATE = @MARGINDATE
SELECT @TRADEDATE = MAX(START_DATE) FROM SETT_MST WHERE SEC_PAYIN >= @MARGINDATE AND SEC_PAYIN <= @MARGINDATE + ' 23:59:59' AND SETT_TYPE = 'M'

SELECT @PREVTRADEDATE = MAX(START_DATE) FROM SETT_MST WHERE START_DATE < @MARGINDATE AND SETT_TYPE = 'M'

CREATE TABLE #OLDMARGIN
( 
	PARTY_CODE	VARCHAR(10),
	MARGIN_AMT	NUMERIC(18,4),
	COLLECT_AMT	NUMERIC(18,4)
)

CREATE TABLE #OLDMARGIN_NEW
( 
	START_DATE	DATETIME,
	PARTY_CODE	VARCHAR(10),
	MARGIN_AMT	NUMERIC(18,4),
	COLLECT_AMT	NUMERIC(18,4)
)

CREATE TABLE #MTOMLOSS
(
	PARTY_CODE	VARCHAR(10),
	MTOMLOSS	NUMERIC(18,4),
	MARGIN	NUMERIC(18,4)
)

/*
IF (SELECT ISNULL(COUNT(1),0) FROM SETT_MST WHERE SEC_PAYIN LIKE @MARGINDATE + '%' AND SETT_TYPE = 'M') > 0 
BEGIN
	INSERT INTO #MTOMLOSS
	SELECT PARTY_CODE,MTOMLOSS,0 FROM ANGELFO.NSEFO.DBO.FOMARGINNEW WHERE MDATE = @PREVTRADEDATE AND MTOMLOSS > 0 
	INSERT INTO #MTOMLOSS 
	SELECT PARTY_CODE,MTOMLOSS,0 FROM ANGELFO.NSECURFO.DBO.FOMARGINNEW WHERE MDATE = @PREVTRADEDATE AND MTOMLOSS > 0 
	INSERT INTO #MTOMLOSS
	SELECT PARTY_CODE, MTOMMARGIN,0 FROM ANGELCOMMODITY.MCDX.DBO.FOMARGINNEW_DATA WHERE MDATE = @PREVTRADEDATE AND MTOMMARGIN > 0 
	INSERT INTO #MTOMLOSS
	SELECT  PARTY_CODE, MTOMMARGIN,0  FROM ANGELCOMMODITY.NCDX.DBO.FOMARGINNEW_DATA WHERE MDATE = @PREVTRADEDATE AND MTOMMARGIN > 0 
END
*/

SELECT PARTY_CODE, MTOMLOSS = SUM(MTOMLOSS), MARGIN = SUM(MARGIN) INTO #MTOMLOSS_FINAL FROM #MTOMLOSS
GROUP BY PARTY_CODE

IF @TRADEDATE <> @MARGINDATE
BEGIN
/*
	INSERT INTO #OLDMARGIN
	SELECT PARTY_CODE, MARGIN_AMT=SUM(MINIMUM_MARGIN), 0 FROM TBL_MG02 T, SETT_MST S
	WHERE MARGIN_DATE = START_DATE
	AND T.SETT_NO = S.SETT_NO AND T.SETT_TYPE = S.SETT_TYPE
	--AND START_DATE = @TRADEDATE
	AND SEC_PAYIN >= @MARGINDATE AND SEC_PAYIN <= @MARGINDATE + ' 23:59:59'
	AND NETQTY > 0 
	GROUP BY PARTY_CODE

	SELECT PARTY_CODE, T_MARGINAVL INTO #OLDMARGIN_RPT 
	FROM TBL_COMBINE_REPORTING_PEAK_DETAIL 
	WHERE MARGINDATE = @TRADEDATE
	AND EXCHANGE = 'NSE' AND SEGMENT = 'CAPITAL'

	UPDATE #OLDMARGIN SET COLLECT_AMT = T_MARGINAVL FROM #OLDMARGIN_RPT
	WHERE #OLDMARGIN.PARTY_CODE = #OLDMARGIN_RPT.PARTY_CODE

	UPDATE #OLDMARGIN SET MARGIN_AMT = (CASE WHEN MARGIN_AMT + COLLECT_AMT > 0 
											 THEN MARGIN_AMT + COLLECT_AMT 
											 ELSE 0 
										END)
	WHERE COLLECT_AMT < 0 */

	/* Changes done as per updation request 
DECLARE @FROMDATE VARCHAR(11),
	@TODATE VARCHAR(11)

INSERT INTO #OLDMARGIN_NEW
	SELECT START_DATE, PARTY_CODE, MARGIN_AMT=SUM(MINIMUM_MARGIN), 0 FROM TBL_MG02 T, SETT_MST S
	WHERE MARGIN_DATE = START_DATE
	AND T.SETT_NO = S.SETT_NO AND T.SETT_TYPE = S.SETT_TYPE
	--AND START_DATE = @TRADEDATE
	AND SEC_PAYIN >= @MARGINDATE AND SEC_PAYIN <=@MARGINDATE + ' 23:59:59'
	AND NETQTY > 0 
	GROUP BY START_DATE, PARTY_CODE
	 
	SELECT @FROMDATE = MIN(START_DATE), @TODATE = MAX(START_DATE) FROM #OLDMARGIN_NEW
	SELECT MARGINDATE, PARTY_CODE, T_MARGINAVL INTO #OLDMARGIN_NEW_RPT 
	FROM TBL_COMBINE_REPORTING_PEAK_DETAIL 
	WHERE MARGINDATE >= @FROMDATE AND MARGINDATE <= @TODATE
	AND EXCHANGE = 'NSE' AND SEGMENT = 'CAPITAL'

	UPDATE #OLDMARGIN_NEW SET COLLECT_AMT = T_MARGINAVL FROM #OLDMARGIN_NEW_RPT
	WHERE #OLDMARGIN_NEW.PARTY_CODE = #OLDMARGIN_NEW_RPT.PARTY_CODE
	AND #OLDMARGIN_NEW.START_DATE = MARGINDATE

	UPDATE #OLDMARGIN_NEW SET MARGIN_AMT = (CASE WHEN MARGIN_AMT + COLLECT_AMT > 0 
											 THEN MARGIN_AMT + COLLECT_AMT 
											 ELSE 0 
										END)
	WHERE COLLECT_AMT < 0 
	 */

	INSERT INTO #OLDMARGIN
	SELECT PARTY_CODE, MARGIN_AMT = SUM(MARGIN_AMT),  COLLECT_AMT = SUM(COLLECT_AMT)
	FROM  #OLDMARGIN_NEW
	GROUP BY PARTY_CODE   
END

/*
SELECT @VARKEY = DETAILKEY FROM VARCONTROL 
WHERE RECDATE >= @MARGINDATE AND RECDATE <= @MARGINDATE + ' 23:59'
  
SELECT SETT_NO,SETT_TYPE,PARTY_CODE,SCRIP_CD,SERIES,QTY=NETQTY,PAYQTY=0,MTOM,NETAMT,
ADNL_MARGIN=CONVERT(NUMERIC(18,4),0),
ADNL_PER=CONVERT(NUMERIC(18,4),0)
INTO #PAYIN
FROM TBL_MG02 D 
WHERE D.MARGIN_DATE = @MARGINDATE
AND MTOM <> 0 

UPDATE #PAYIN SET ADNL_PER = INDEXVAR
FROM VARDETAIL D WHERE DETAILKEY =@VARKEY AND D.SCRIP_CD = #PAYIN.SCRIP_CD AND D.SERIES = #PAYIN.SERIES

UPDATE #PAYIN SET ADNL_MARGIN = ABS(NETAMT*ADNL_PER) / 100
WHERE QTY < 0 

SELECT DISTINCT SETT_NO, SETT_TYPE INTO #SETT FROM #PAYIN 

UPDATE #PAYIN SET PAYQTY = D.QTY 
FROM (SELECT D.SETT_NO, D.SETT_TYPE, PARTY_CODE,SCRIP_CD,D.SERIES,QTY = SUM(QTY)
	  FROM ANGELDEMAT.MSAJAG.DBO.DELTRANS D, #SETT S
	  WHERE D.SETT_NO = S.SETT_NO 
	  AND D.SETT_TYPE = S.SETT_TYPE
	  AND FILLER2 = 1 AND DRCR = 'C' AND SHARETYPE <> 'AUCTION'
	  GROUP BY  D.SETT_NO, D.SETT_TYPE, PARTY_CODE,SCRIP_CD,D.SERIES ) D
WHERE D.SETT_NO = #PAYIN.SETT_NO 
	  AND D.SETT_TYPE = #PAYIN.SETT_TYPE
	  AND D.PARTY_CODE = #PAYIN.PARTY_CODE 
	  AND D.SCRIP_CD = #PAYIN.SCRIP_CD 
	  AND D.SERIES = #PAYIN.SERIES 
	  AND #PAYIN.QTY < 0 

SELECT PARTY_CODE, COLLECT=SUM(PRADNYA.DBO.FNMIN(MTOM,COLLECT))+CONVERT(NUMERIC(18,2),SUM(ADNL_MARGIN)) INTO #MTMCOLLECT
FROM (
SELECT SETT_NO,SETT_TYPE,PARTY_CODE,MTOM=(CASE WHEN SUM(MTOM) < 0 THEN ABS(SUM(MTOM)) ELSE 0 END),
COLLECT=SUM(CASE WHEN QTY < 0 AND MTOM < 0 THEN ABS(PAYQTY*(MTOM/ABS(QTY))) ELSE 0 END),
ADNL_MARGIN = SUM(CASE WHEN QTY < 0 AND ADNL_MARGIN > 0 THEN ABS(PAYQTY*(ADNL_MARGIN/ABS(QTY))) ELSE 0 END)
FROM #PAYIN 
GROUP BY SETT_NO,SETT_TYPE,PARTY_CODE ) A
GROUP BY PARTY_CODE
*/ 

ALTER TABLE TBL_COMBINE_REPORTING_PEAK_TDAY REBUILD  
ALTER TABLE TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL REBUILD 
	
SELECT * INTO #V2_TBL_EXCHANGE_MARGIN_REPORT_PEAK FROM V2_TBL_EXCHANGE_MARGIN_REPORT_PEAK
WHERE @MARGINDATE BETWEEN FROM_DATE AND TO_DATE
  
IF @CHECKFLAG = 0  
  
BEGIN    
 DELETE FROM TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL    
 WHERE MARGINDATE = @MARGINDATE OPTION(MAXDOP 4)
  
 CREATE TABLE #MTFADJUST
(
	PARTY_CODE	VARCHAR(10),
	MARGIN_REQ	NUMERIC(18,4),
	MTOM_LOSS NUMERIC(18,4),
	EXCESS_COLL	NUMERIC(18,4),
	TDAY_MARGIN_REQ	NUMERIC(18,4),
	CASH_MARGIN_ADJUST	NUMERIC(18,4)
)
 
SELECT MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, TDAY_MARGIN, FILECOUNTER=0, PEAK_EOD = CONVERT(NUMERIC(18,4),0),
DEBITBILL= CONVERT(NUMERIC(18,4),0),LEDBAL= CONVERT(NUMERIC(18,4),0) INTO #PEAKSNAPSHOT FROM TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL
WHERE 1 =2 

SELECT MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, TDAY_MARGIN, FILECOUNTER=0 INTO #PEAKEOD FROM TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL
WHERE 1 =2 

SET @MRGCUR = CURSOR FOR             
    SELECT             
    MARGSQL = ' INSERT INTO #PEAKSNAPSHOT (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, TDAY_MARGIN, FILECOUNTER )'    
    +  ' SELECT ''' + @MARGINDATE + ''',''' + T.EXCHANGE + ''', ''' + T.SEGMENT + ''',PARTY_CODE,MARGIN_CALL=SUM(ISNULL(' + (CASE WHEN @FORREPORTING = 'Y' THEN REPLACE(MARGINCOLUMNSNONCASH,'ADDMARGIN',0) ELSE MARGINCOLUMNSNONCASH END) + ',0)),FILECOUNTER FROM ' +              
    SHARESERVER + '.'+ SHAREDB + '.DBO.' + MARGINTABLE +              
   ' WHERE ' + MARGINDATEFIELD + ' = (SELECT MAX(' + MARGINDATEFIELD + ') FROM ' + SHARESERVER + '.'+ SHAREDB + '.DBO.' + MARGINTABLE + ' WHERE ' + MARGINDATEFIELD + ' <= ''' +  @MARGINDATE +' 23:59'') AND PARTY_CODE <> ''12798'' GROUP BY PARTY_CODE, FILECOUNTER'            
    FROM V2_TBL_EXCHANGE_MARGIN_REPORT_PEAK_INTRADAY T, PRADNYA..MULTICOMPANY M            
    WHERE T.EXCHANGE = M.EXCHANGE            
    AND T.SEGMENT = M.SEGMENT     
    AND M.SHARESERVER <> 'ANGELDEMAT'          
    AND T.SEGMENT <> 'POA'          
    AND @MARGINDATE BETWEEN FROM_DATE AND TO_DATE
   OPEN @MRGCUR            
  
   FETCH NEXT FROM @MRGCUR INTO @STRSQL            
   WHILE @@FETCH_STATUS = 0                   
   BEGIN                
  
    EXEC (@STRSQL)        
	--PRINT @STRSQL
    FETCH NEXT FROM @MRGCUR INTO @STRSQL            
  
   END             
  
   CLOSE @MRGCUR            
  
   DEALLOCATE @MRGCUR    
   
   
SET @MRGCUR = CURSOR FOR             
    SELECT             
    MARGSQL = ' INSERT INTO #PEAKEOD (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, TDAY_MARGIN, FILECOUNTER )'    
    +  ' SELECT ''' + @MARGINDATE + ''',''' + T.EXCHANGE + ''', ''' + T.SEGMENT + ''',PARTY_CODE,MARGIN_CALL=SUM(ISNULL(' + (CASE WHEN @FORREPORTING = 'Y' THEN REPLACE(MARGINCOLUMNSNONCASH,'ADDMARGIN',0) ELSE MARGINCOLUMNSNONCASH END) + ',0)),FILECOUNTER=0 FROM ' +              
    SHARESERVER + '.'+ SHAREDB + '.DBO.' + MARGINTABLE +              
   ' WHERE ' + MARGINDATEFIELD + ' = (SELECT MAX(' + MARGINDATEFIELD + ') FROM ' + SHARESERVER + '.'+ SHAREDB + '.DBO.' + MARGINTABLE + ' WHERE ' + MARGINDATEFIELD + ' <= ''' +  @MARGINDATE +' 23:59'' AND PARTY_CODE <> ''12798'')  AND PARTY_CODE <> ''12798'' GROUP BY PARTY_CODE'            
    FROM V2_TBL_EXCHANGE_MARGIN_REPORT_PEAK T, PRADNYA..MULTICOMPANY M            
    WHERE T.EXCHANGE = M.EXCHANGE            
    AND T.SEGMENT = M.SEGMENT     
    AND M.SHARESERVER <> 'ANGELDEMAT'          
    AND T.SEGMENT <> 'POA'           
	and MARGINCOLUMNSNONCASH = 'peakmargin'
	AND @margindate between FROM_DATE and TO_DATE
   OPEN @MRGCUR            
  
   FETCH NEXT FROM @MRGCUR INTO @STRSQL            
   WHILE @@FETCH_STATUS = 0                   
   BEGIN                
        
	--PRINT @STRSQL
    EXEC (@STRSQL)  
    FETCH NEXT FROM @MRGCUR INTO @STRSQL            
  
   END             
  
   CLOSE @MRGCUR            
  
   DEALLOCATE @MRGCUR  
   
   
SET @MRGCUR = CURSOR FOR             
    SELECT             
    MARGSQL = ' INSERT INTO #PEAKSNAPSHOT (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, TDAY_MARGIN, FILECOUNTER )'    
    +  ' SELECT ''' + @MARGINDATE + ''',''' + T.EXCHANGE + ''', ''' + T.SEGMENT + ''',PARTY_CODE,MARGIN_CALL=SUM(ISNULL(' + (CASE WHEN @FORREPORTING = 'Y' THEN REPLACE(MARGINCOLUMNSNONCASH,'ADDMARGIN',0) ELSE MARGINCOLUMNSNONCASH END) + ',0)),FILECOUNTER=5 FROM ' +              
    SHARESERVER + '.'+ SHAREDB + '.DBO.' + MARGINTABLE +              
   ' WHERE ' + MARGINDATEFIELD + ' = (SELECT MAX(' + MARGINDATEFIELD + ') FROM ' + SHARESERVER + '.'+ SHAREDB + '.DBO.' + MARGINTABLE + ' WHERE ' + MARGINDATEFIELD + ' <= ''' +  @MARGINDATE +' 23:59'') AND PARTY_CODE <> ''12798'' GROUP BY PARTY_CODE'            
    FROM V2_TBL_EXCHANGE_MARGIN_REPORT T, PRADNYA..MULTICOMPANY M            
    WHERE T.EXCHANGE = M.EXCHANGE            
    AND T.SEGMENT = M.SEGMENT     
    AND M.SHARESERVER <> 'ANGELDEMAT'          
    AND T.SEGMENT <> 'POA'           
	--and MARGINCOLUMNSNONCASH = 'peakmargin'
	and t.Exchange not in('mcx','ncx')
   OPEN @MRGCUR            
  
   FETCH NEXT FROM @MRGCUR INTO @STRSQL            
   WHILE @@FETCH_STATUS = 0                   
   BEGIN                
  
    EXEC (@STRSQL)        
	--PRINT @STRSQL
    FETCH NEXT FROM @MRGCUR INTO @STRSQL            
  
   END             
   
  UPDATE #PEAKSNAPSHOT SET TDAY_MARGIN = CONVERT(NUMERIC(18,2),TDAY_MARGIN * PEAK_PER / 100)
  FROM  V2_TBL_EXCHANGE_MARGIN_REPORT_PEAK_INTRADAY P
  WHERE #PEAKSNAPSHOT.EXCHANGE = P.EXCHANGE 
  AND #PEAKSNAPSHOT.SEGMENT = P.SEGMENT  
  AND @MARGINDATE BETWEEN FROM_DATE AND TO_DATE

  SELECT MARGINDATE, PARTY_CODE, TDAY_MARGIN=SUM(TDAY_MARGIN), FILECOUNTER 
  INTO #MAXPEAK
  FROM #PEAKSNAPSHOT
  WHERE FILECOUNTER <= 4
  GROUP BY MARGINDATE, PARTY_CODE,FILECOUNTER 
    
  UPDATE #PEAKEOD SET TDAY_MARGIN = CONVERT(NUMERIC(18,2),TDAY_MARGIN * PEAK_PER / 100)
  FROM  V2_TBL_EXCHANGE_MARGIN_REPORT_PEAK P
  WHERE #PEAKEOD.EXCHANGE = P.EXCHANGE 
  AND #PEAKEOD.SEGMENT = P.SEGMENT
  AND @MARGINDATE BETWEEN FROM_DATE AND TO_DATE
  
  UPDATE #PEAKSNAPSHOT SET PEAK_EOD = 0

  UPDATE #PEAKSNAPSHOT SET PEAK_EOD = P.TDAY_MARGIN
  FROM #PEAKEOD P
  WHERE P.PARTY_CODE = #PEAKSNAPSHOT.PARTY_CODE
  AND #PEAKSNAPSHOT.FILECOUNTER = 1
  AND P.EXCHANGE = 'NSE' AND P.SEGMENT = 'CAPITAL'
  
  SELECT CLTCODE, VAMT=SUM(CASE WHEN DRCR = 'D' THEN VAMT ELSE -VAMT END) INTO #DBILL_NRM FROM ACCOUNT.DBO.LEDGER L(NOLOCK),  #PEAKSNAPSHOT P
  WHERE EDT LIKE @MARGINDATE + '%' AND VTYP =15 AND DRCR = 'D'
  AND P.PARTY_CODE = L.CLTCODE AND P.EXCHANGE = 'NSE' AND P.SEGMENT = 'CAPITAL'
  AND PEAK_EOD = P.TDAY_MARGIN AND TDAY_MARGIN > 0 AND 1 = 2
  GROUP BY CLTCODE

  INSERT INTO #DBILL_NRM 
  SELECT CLTCODE, VAMT=SUM(CASE WHEN DRCR = 'D' THEN VAMT ELSE -VAMT END) FROM ACCOUNT.DBO.LEDGER L(NOLOCK),  #PEAKSNAPSHOT P
  WHERE EDT LIKE @MARGINDATE + '%' AND VTYP =35 AND DRCR = 'C'
  AND P.PARTY_CODE = L.CLTCODE AND P.EXCHANGE = 'NSE' AND P.SEGMENT = 'CAPITAL'
  AND PEAK_EOD = P.TDAY_MARGIN AND TDAY_MARGIN > 0 AND 1 = 2
  GROUP BY CLTCODE
   
  /*
  SELECT @PREVDATE = MAX(TRANSDATE) FROM TBL_EPN_BENEFIT
  WHERE TRANSDATE < @MARGINDATE  

  INSERT INTO #DBILL_NRM
  SELECT PARTY_CODE, VAMT=SUM(CASHBENEFIT)
  FROM TBL_EPN_BENEFIT E
  WHERE TRANSDATE = @PREVDATE
  AND SETT_NO IN (SELECT SETT_NO FROM SETT_MST S WHERE S.SETT_NO = E.SETT_NO AND S.SETT_TYPE = E.SETT_TYPE
  AND SEC_PAYIN = @MARGINDATE)
  GROUP BY PARTY_CODE
  */
  SELECT CLTCODE, VAMT = SUM(VAMT) 
  INTO #DBILL
  FROM #DBILL_NRM
  GROUP BY CLTCODE
  HAVING SUM(VAMT) > 0 

  UPDATE #PEAKSNAPSHOT sET DEBItBILL= 0, LEDBAL = 0

  UPDATE #PEAKSNAPSHOT sET DEBItBILL = vAMT 
  fROM #dbill
  where #dbill.cltcode = party_code

  SELECT EXCHANGE, SEGMENT, FILECOUNTER = MAX(FILECOUNTER) INTO #PEAKSEGMENT FROM #PEAKSNAPSHOT 
  WHERE EXCHANGE NOT IN ('MCX', 'NCX') 
  GROUP BY EXCHANGE, SEGMENT 

  DELETE FROM #PEAKSNAPSHOT
  WHERE FILECOUNTER >= 5
  AND PARTY_CODE NOT IN (SELECT PARTY_CODE FROM #PEAKSNAPSHOT
						 WHERE FILECOUNTER > 4 AND EXCHANGE IN ('MCX', 'NCX'))
  DECLARE @FILECNT INT,
		  @FILECUR	CURSOR

  SET @FILECUR = CURSOR FOR 
  SELECT DISTINCT FILECOUNTER FROM #PEAKSNAPSHOT
  WHERE FILECOUNTER > 4 AND EXCHANGE IN ('MCX', 'NCX') 
  ORDER BY 1
  OPEN @FILECUR
  FETCH NEXT FROM @FILECUR INTO @FILECNT
  WHILE @@FETCH_STATUS = 0 
  BEGIN
	INSERT INTO #PEAKSEGMENT
	SELECT 'MCX', 'FUTURES', @FILECNT 
	INSERT INTO #PEAKSEGMENT
	SELECT 'NCX', 'FUTURES', @FILECNT 

	INSERT INTO #MAXPEAK
	SELECT MARGINDATE, PARTY_CODE, TDAY_MARGIN=SUM(TDAY_MARGIN),FILECOUNTER=@FILECNT
	FROM #PEAKSNAPSHOT P, #PEAKSEGMENT P1
	WHERE P.EXCHANGE = P1.EXCHANGE 
	AND P.SEGMENT = P1.SEGMENT 
	AND P.FILECOUNTER = P1.FILECOUNTER 
	GROUP BY MARGINDATE, PARTY_CODE

	DELETE FROM #PEAKSEGMENT
	WHERE EXCHANGE IN ('MCX', 'NCX')

	FETCH NEXT FROM @FILECUR INTO @FILECNT
  END
  CLOSE @FILECUR
  DEALLOCATE @FILECUR 

  SELECT MARGINDATE, PARTY_CODE, TDAY_MARGIN = MAX(TDAY_MARGIN), EOD_PEAK=CONVERT(NUMERIC(18,4),0), OldMargin=CONVERT(NUMERIC(18,4),0)
  INTO #PEAK FROM (
  SELECT MARGINDATE, PARTY_CODE, TDAY_MARGIN=SUM(TDAY_MARGIN),FILECOUNTER FROM #MAXPEAK 
  GROUP BY MARGINDATE, PARTY_CODE,FILECOUNTER ) A 
  GROUP BY MARGINDATE, PARTY_CODE

  UPDATE #PEAK SET EOD_PEAK = P.TDAY_MARGIN
  FROM #PEAKEOD P
  WHERE P.PARTY_CODE = #PEAK.PARTY_CODE 
  AND P.EXCHANGE = 'NSE' AND P.SEGMENT = 'CAPITAL'


  UPDATE #PEAK SET OLDMARGIN = P.MARGIN_AMT
  FROM #OLDMARGIN P
  WHERE P.PARTY_CODE = #PEAK.PARTY_CODE  
  AND EOD_PEAK = TDAY_MARGIN

  --SELECT * FROM #PEAK 
  --WHERE PARTY_CODE='A112317'

  /*
  SELECT * FROM #MAXPEAK
  WHERE PARTY_CODE='D51560'
  SELECT * FROM #PEAKSNAPSHOT
  WHERE PARTY_CODE='D51560'
  SELECT * FROM #PEAKEOD
  WHERE PARTY_CODE='D51560'


  RETURN */

  CREATE INDEX #PK
  ON #PEAK (PARTY_cODE)

 SET @CNT = 1  
  
 SET @DAY = 1   
  
  
IF CONVERT(DATETIME,@MARGINDATE) <= CONVERT(DATETIME,'SEP 16 2020')
BEGIN
	INSERT INTO #MTFADJUST
	EXEC MTFTRADE.DBO.TO_CHK_2DAY @MARGINDATE, 1 
END
 
   
 SET @MARGINDATECURSOR = CURSOR FOR  
  
  
  /* 
 SELECT DISTINCT TOP 3 SYSDATE FROM MSAJAG.DBO.CLOSING   
 WHERE SYSDATE >= @MARGINDATE   
 ORDER BY SYSDATE  

  */ 
   
   
   
 --/*    
  
 SELECT TOP 1 START_DATE FROM (    
 SELECT DISTINCT Start_Date FROM AccBill WHERE Start_Date >=@MARGINDATE AND SETT_TYPE='M'  
UNION 
SELECT DISTINCT TOP 4 TRADE_DATE=CONVERT(DATETIME,LEFT(TRADE_DATE,11)) FROM ANGELCOMMODITY.MCDX.DBO.FOCLOSING WHERE TRADE_DATE >=@MARGINDATE )A    
 ORDER BY 1  
    
   --*/    
  
  
  
    /*  
SELECT DISTINCT TOP 3 START_DATE SYSDATE FROM MSAJAG.DBO.SETT_MST     
 --WHERE START_DATE >= @MARGINDATE  AND SETT_TYPE ='N'  
 --ORDER BY START_DATE   
 */  
     
   
  
  
 OPEN  @MARGINDATECURSOR  
  
 FETCH NEXT FROM @MARGINDATECURSOR INTO @MARGINDATENEW  
  
 WHILE @@FETCH_STATUS = 0  
  
 BEGIN  
  
  SET @EXCHANGEWISE_CURSOR = CURSOR  
  
  FOR  
  
  SELECT DISTINCT   
  
   EXCHANGE = MAX(M.EXCHANGE),  
  
   SEGMENT = MAX(M.SEGMENT),  
  
   SHARESERVER,  
  
   ACCOUNTDB,  
  
   ACCOUNTSERVER  
  
  FROM PRADNYA..MULTICOMPANY M(NOLOCK)   
  
  WHERE  M.SEGMENT NOT IN ('SLBS')  
  
  and SHARESERVER <> 'ANGELDEMAT'  
  
   AND SEGMENT_DESCRIPTION NOT LIKE '%PCM%'   
  
   AND PRIMARYSERVER = 1   
  
   AND EXISTS (SELECT T.EXCHANGE FROM #V2_TBL_EXCHANGE_MARGIN_REPORT_PEAK T   
  
      WHERE T.EXCHANGE = M.EXCHANGE  
  
      AND T.SEGMENT = M.SEGMENT AND @MARGINDATE BETWEEN FROM_DATE AND TO_DATE)  
  
  GROUP BY SHARESERVER, ACCOUNTDB, ACCOUNTSERVER   
  
  OPEN @EXCHANGEWISE_CURSOR  
  
  FETCH NEXT  
  
  FROM @EXCHANGEWISE_CURSOR  
  
  INTO  
  
   @EXCHANGE_NEW,  
  
   @SEGMENT_NEW,  
  
   @SHARESERVER,  
  
   @ACCOUNTDB,  
  
   @ACCOUNTSERVER  
  
  
  
  WHILE @@FETCH_STATUS = 0  
  
  BEGIN  
  
   SET @STRSQL = ''  
  
   IF @ACCOUNTSERVER = 'AngelNSECM'   
  
   BEGIN  
  
    SET @ACCOUNTSERVER =''  
  
   END  
  
   ELSE  
  
   BEGIN  
  
    SET @ACCOUNTSERVER = @ACCOUNTSERVER + '.'  
  
   END   
  
     
  
   SET @STRSQL = 'EXEC ' + @ACCOUNTSERVER + @ACCOUNTDB + '.DBO.PR_GET_LEDBAL_COMBINE '''  + @MARGINDATENEW + ''', ' + @PEAK +' '   
  
   EXEC (@STRSQL)   
  
  
  
   IF @CNT = 1  
  
   BEGIN  
  
    SET @STRSQL = ' INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, TDAY_LEDGER )'  
  
    SET @STRSQL = @STRSQL + ' SELECT ''' + @MARGINDATE + ''', ''' + @EXCHANGE_NEW + ''', ''' + @SEGMENT_NEW + ''', PARTY_CODE, LEDBAL '  
  
    SET @STRSQL = @STRSQL + ' FROM ' + @ACCOUNTSERVER + @ACCOUNTDB + '.DBO.FOMARGINNEW_LEDGERBAL WHERE MARGINDATE = ''' + @MARGINDATENEW + ''' '   
  
    EXEC (@STRSQL)  
	
   END  
  
  
  
   IF @CNT = 2  
  
   BEGIN  
  
    SET @STRSQL = ' INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, T1DAY_LEDGER )'  
  
    SET @STRSQL = @STRSQL + ' SELECT ''' + @MARGINDATE + ''', ''' + @EXCHANGE_NEW + ''', ''' + @SEGMENT_NEW + ''', PARTY_CODE, LEDBAL '  
  
    SET @STRSQL = @STRSQL + ' FROM ' + @ACCOUNTSERVER + @ACCOUNTDB + '.DBO.FOMARGINNEW_LEDGERBAL WHERE MARGINDATE = ''' + @MARGINDATENEW + ''' '   
  
    EXEC (@STRSQL)  
  
   END  
  
  
  
   IF @CNT = 3  
  
   BEGIN  
  
    SET @STRSQL = ' INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, T2DAY_LEDGER )'  
  
    SET @STRSQL = @STRSQL + ' SELECT ''' + @MARGINDATE + ''', ''' + @EXCHANGE_NEW + ''', ''' + @SEGMENT_NEW + ''', PARTY_CODE, LEDBAL '  
  
    SET @STRSQL = @STRSQL + ' FROM ' + @ACCOUNTSERVER + @ACCOUNTDB + '.DBO.FOMARGINNEW_LEDGERBAL WHERE MARGINDATE = ''' + @MARGINDATENEW + ''' '   
  
    EXEC (@STRSQL)  

  
   END  
  
   FETCH NEXT  
  
   FROM @EXCHANGEWISE_CURSOR  
  
   INTO   
  
    @EXCHANGE_NEW,  
  
    @SEGMENT_NEW,  
  
    @SHARESERVER,  
  
    @ACCOUNTDB,  
  
    @ACCOUNTSERVER  
  
  END  
  
  
  CLOSE @EXCHANGEWISE_CURSOR  
     /*
	UPDATE #PEAKSNAPSHOT SET LEDBAL = TDAY_LEDGER FROM 
	(SELECT PARTY_CODE,TDAY_LEDGER = SUM(ISNULL(TDAY_LEDGER,0)) FROM TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL T
	WHERE T.MARGINDATE = @MARGINDATE   -- 
	AND TDAY_LEDGER is NOT NULL 
	GROUP BY PARTY_CODE)A
	WHERE #PEAKSNAPSHOT.PARTY_CODE = A.PARTY_CODE	
	AND EXCHANGE='NSE' AND SEGMENT ='CAPITAL'

	INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, TDAY_LEDGER )
	SELECT @MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE,  
	TDAY_LEDGER = (CASE WHEN LEDBAL < 0 THEN PRADNYA.DBO.FNMIN(DEBITBILL,ABS(LEDBAL)) ELSE 0 END) 
	+ PRADNYA.DBO.FNMIN(DEBITBILL,TDAY_MARGIN) 
	FROM #PEAKSNAPSHOT
	WHERE TDAY_MARGIN = PEAK_EOD
	AND TDAY_MARGIN > 0  
	AND EXCHANGE='NSE' 
	AND SEGMENT = 'CAPITAL'
	AND FILECOUNTER = 1
		
	INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, TDAY_LEDGER )
	SELECT @MARGINDATE, 'NSE', 'CAPITAL', CLTCODE, SUM(VAMT)
	FROM ACCOUNT.DBO.LEDGER 
	WHERE VDT = @MARGINDATE AND EDT = @MARGINDATE
	AND VTYP = 35 AND DRCR = 'D'
	GROUP BY  CLTCODE*/

  IF @DAY = 1   
  
  BEGIN  
  
   INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, TDAY_CASHCOLL, TDAY_FDBG, TDAY_NONCASH)  
  
   SELECT MARGINDATE=@MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE,   
  
   TDAY_CASHCOLL = ISNULL(SUM(CASE WHEN COLL_TYPE = 'MARGIN' THEN ISNULL(MRG_FINALAMOUNT,0) ELSE 0 END),0),  
  
   TDAY_FDBG = ISNULL(SUM(CASE WHEN COLL_TYPE = 'BG' OR COLL_TYPE = 'FD' THEN ISNULL(MRG_FINALAMOUNT,0) ELSE 0 END),0),  
  
   TDAY_NONCASH = ISNULL(SUM(CASE WHEN COLL_TYPE = 'SEC' THEN ISNULL(MRG_FINALAMOUNT,0) ELSE 0 END),0)  
  
   FROM  V2_TBL_COLLATERAL_MARGIN_COMBINE M  
  
   WHERE EFFDATE = @MARGINDATENEW  
  
   AND EXISTS (SELECT T.EXCHANGE FROM #V2_TBL_EXCHANGE_MARGIN_REPORT_PEAK T   
  
    WHERE T.EXCHANGE = M.EXCHANGE  
  
    AND T.SEGMENT = M.SEGMENT
	AND @MARGINDATE BETWEEN FROM_DATE AND TO_DATE)  
  
   GROUP BY EXCHANGE, SEGMENT, PARTY_CODE  

   
   INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, TDAY_CASHCOLL, TDAY_FDBG, TDAY_NONCASH)  
  
   SELECT MARGINDATE=@MARGINDATE, EXCHANGE='NSE', SEGMENT='FUTURES', PARTY_CODE,   
  
   TDAY_CASHCOLL = ISNULL(SUM(CASE WHEN COLL_TYPE = 'MARGIN' THEN ISNULL(MRG_FINALAMOUNT,0) ELSE 0 END),0),  
  
   TDAY_FDBG = ISNULL(SUM(CASE WHEN COLL_TYPE = 'BG' OR COLL_TYPE = 'FD' THEN ISNULL(MRG_FINALAMOUNT,0) ELSE 0 END),0),  
  
   TDAY_NONCASH = ISNULL(SUM(CASE WHEN COLL_TYPE = 'SEC' THEN ISNULL(MRG_FINALAMOUNT,0) ELSE 0 END),0)  
  
   FROM  V2_TBL_COLLATERAL_MARGIN_COMBINE M  
  
   WHERE EFFDATE = @MARGINDATENEW  
   AND SEGMENT='PEAK' 
  
   GROUP BY EXCHANGE, SEGMENT, PARTY_CODE  
   
	SELECT @EPNDATE = ISNULL(MAX(START_DATE),@MARGINDATENEW) FROM SETT_MST WHERE START_DATE <= @MARGINDATENEW AND SETT_TYPE='M'
	
	SELECT top 0 TRANSDATE=@MARGINDATE, PARTY_CODE, MARGIN_BENEFIT = SUM(ROUND(MARGIN_BENEFIT,2)), CASHBENEFIT = SUM(CASHBENEFIT)
	INTO #EPNPOOL_PT
	FROM TBL_EPN_BENEFIT
	WHERE TRANSDATE = @EPNDATE
	AND MARGIN_BENEFIT + CASHBENEFIT > 0 
	GROUP BY TRANSDATE, PARTY_CODE
	/***** Added for Direct Payout / Late payout data Start ********/
	IF (@EPNDATE<'FEB  9 2025')  
	BEGIN
		INSERT  INTO #EPNPOOL_PT
	SELECT TRANSDATE=@MARGINDATE, PARTY_CODE, MARGIN_BENEFIT = SUM(ROUND(MARGIN_BENEFIT,2)), CASHBENEFIT = SUM(CASHBENEFIT)
	FROM TBL_EPN_BENEFIT
	WHERE TRANSDATE = @EPNDATE
	AND MARGIN_BENEFIT + CASHBENEFIT > 0 
	GROUP BY TRANSDATE, PARTY_CODE
	END
	ELSE
	BEGIN
		INSERT  INTO #EPNPOOL_PT
	SELECT TRANSDATE=@MARGINDATE, PARTY_CODE, MARGIN_BENEFIT = SUM(ROUND(MARGIN_BENEFIT,2)), CASHBENEFIT = SUM(CASHBENEFIT)
	FROM TBL_EPN_BENEFIT_PEAK
	WHERE TRANSDATE = @EPNDATE
	AND MARGIN_BENEFIT + CASHBENEFIT > 0 
	GROUP BY TRANSDATE, PARTY_CODE
	END

	CREATE INDEX #PARTY ON #EPNPOOL_PT (PARTY_CODE)


	INSERT INTO #EPNPOOL_PT
	SELECT TR_DATE,PARTY_CODE,0,CASHBENEFIT=SUM(BENIFIT+MARGIN+ADDNL_MAR)  FROM TBL_EPN_DIRECT_PAYOUT WHERE TR_DATE=@MARGINDATE
	GROUP BY  TR_DATE,PARTY_CODE

	SELECT TRANSDATE=@MARGINDATE, PARTY_CODE, MARGIN_BENEFIT = SUM(ROUND(MARGIN_BENEFIT,2)), CASHBENEFIT = SUM(CASHBENEFIT)
	INTO #EPNPOOL_P FROM #EPNPOOL_PT
	GROUP BY PARTY_CODE

	CREATE INDEX #PARTY ON #EPNPOOL_P (PARTY_CODE)

	/***** Added for Direct Payout / Late payout data End ********/


	UPDATE #EPNPOOL_P SET
	MARGIN_BENEFIT=CONVERT(NUMERIC(18,2),MARGIN_BENEFIT*PEAK_PER/100)
	FROM  #V2_TBL_EXCHANGE_MARGIN_REPORT_PEAK p
	WHERE P.EXCHANGE = 'NSE' AND P.SEGMENT = 'CAPITAL'



	INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, TDAY_NONCASH, TDAY_CASHCOLL)
	SELECT TRANSDATE, EXCHANGE = 'EPN', SEGMENT = 'EPN', PARTY_CODE, TDAY_NONCASH = 0, CASHBENEFIT = SUM(CASHBENEFIT+MARGIN_BENEFIT)
	FROM #EPNPOOL_P 
	GROUP BY TRANSDATE, PARTY_CODE


	
       
	SELECT @PREVDATE = MAX(TRANSDATE) FROM TBL_EPN_BENEFIT
	WHERE TRANSDATE < @EPNDATE  
	 /*
	SELECT @PREVDATE = MAX(TRANSDATE) FROM TBL_EPN_BENEFIT
	WHERE TRANSDATE < @MARGINDATE */

	SELECT PARTY_CODE, CASHBENEFIT=CONVERT(NUMERIC(18,2),SUM(CASHBENEFIT)*20/100),
	MARGIN_BENEFIT	= SUM(MARGIN_BENEFIT),
	TDAY_PEAK = CONVERT(NUMERIC(18,4),0),
	ORG_CASHBENEFIT=CONVERT(NUMERIC(18,2),SUM(CASHBENEFIT) *20/100)
	INTO #EPNREV_P FROM (
	SELECT PARTY_CODE, CASHBENEFIT=-SUM(CASHBENEFIT), MARGIN_BENEFIT=CONVERT(NUMERIC(18,4),0) FROM TBL_EPN_BENEFIT T
	WHERE TRANSDATE = @PREVDATE 
	AND SETT_NO IN (SELECT SETT_NO FROM TBL_EPN_BENEFIT T1
	WHERE TRANSDATE = @EPNDATE AND T.SETT_NO = T1.SETT_NO AND T.SETT_TYPE = T1.SETT_TYPE)
	GROUP BY PARTY_CODE 
	UNION ALL
	SELECT PARTY_CODE, CASHBENEFIT=SUM(CASHBENEFIT),MARGIN_BENEFIT=SUM(MARGIN_BENEFIT) FROM TBL_EPN_BENEFIT T
	WHERE TRANSDATE = @EPNDATE
	GROUP BY PARTY_CODE ) A, #V2_TBL_EXCHANGE_MARGIN_REPORT_PEAK P
	WHERE P.EXCHANGE = 'NSE' AND P.SEGMENT = 'CAPITAL'
	GROUP BY PARTY_CODE, PEAK_PER
	HAVING SUM(CASHBENEFIT+MARGIN_BENEFIT) > 0 

	CREATE INDEX #PARTY ON #EPNREV_P (PARTY_CODE)

	
	/*
	INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, TDAY_NONCASH, TDAY_CASHCOLL)
	SELECT TRANSDATE=@MARGINDATE, EXCHANGE = 'EPN', SEGMENT = 'EPN', PARTY_CODE, TDAY_NONCASH = 0, CASHBENEFIT = -SUM(CASHBENEFIT)
	FROM #EPNREV_P 
	GROUP BY PARTY_CODE*/
       

	--SELECT * FROM #EPNREV_P WHERE PARTY_CODE = 'S262387'
	--SELECT * FROM #EPNPOOL_P WHERE PARTY_CODE = 'S262387'

	UPDATE #EPNREV_P SET MARGIN_BENEFIT = p.MARGIN_BENEFIT + #EPNREV_P.CASHBENEFIT
	FROM #EPNPOOL_P P
	WHERE #EPNREV_P.PARTY_CODE = P.PARTY_CODE

	UPDATE #EPNREV_P SET TDAY_PEAK = PEAKMARGIN 
	FROM TBL_CMMARGIN C WITH(NOLOCK)
	WHERE MARGINDATE = @MARGINDATE
	AND #EPNREV_P.PARTY_CODE = C.PARTY_CODE
	
	--SELECT * FROM #EPNREV_P WHERE PARTY_CODE = 'S262387'

	UPDATE #EPNREV_P SET TDAY_PEAK = CONVERT(NUMERIC(18,2),TDAY_PEAK*PEAK_PER/100), 
	MARGIN_BENEFIT=CONVERT(NUMERIC(18,2),MARGIN_BENEFIT*PEAK_PER/100)
	FROM  #V2_TBL_EXCHANGE_MARGIN_REPORT_PEAK p
	WHERE P.EXCHANGE = 'NSE' AND P.SEGMENT = 'CAPITAL'
	
	UPDATE #EPNREV_P SET CASHBENEFIT = MARGIN_BENEFIT

	--SELECT * FROM #EPNREV_P WHERE PARTY_CODE = 'S262387'

	/*
	INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, TDAY_NONCASH, TDAY_CASHCOLL)
	SELECT @MARGINDATE, EXCHANGE = 'EPN', SEGMENT = 'EPN', PARTY_CODE, TDAY_NONCASH = 0, CASHBENEFIT = -SUM(CASHBENEFIT)
	FROM #EPNREV_P
	GROUP BY PARTY_CODE
	*/
   IF CONVERT(DATETIME,@MARGINDATE) < CONVERT(DATETIME, 'SEP 16 2020')
   BEGIN 
	   IF CONVERT(DATETIME,@MARGINDATE) >= CONVERT(DATETIME, 'JUL 28 2020')
	   BEGIN
		INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, TDAY_CASHCOLL, TDAY_FDBG, TDAY_NONCASH)    
		SELECT MARGINDATE = @MARGINDATE, EXCHANGE = 'NSE', SEGMENT = 'CAPITAL', PARTY_CODE, TDAY_CASHCOLL = 0, TDAY_FDBG = 0, 
		TDAY_NONCASH = CASH_MARGIN_ADJUST
		FROM #MTFADJUST
		WHERE CASH_MARGIN_ADJUST > 0 
	   END
	   IF CONVERT(DATETIME,@MARGINDATE) >= CONVERT(DATETIME, 'MAY 27 2020')
	   BEGIN
			INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, TDAY_CASHCOLL, TDAY_FDBG, TDAY_NONCASH)    
			SELECT MARGINDATE = @MARGINDATE, EXCHANGE = 'MTF', SEGMENT = 'MTF', PARTY_CODE, TDAY_CASHCOLL = 0, TDAY_FDBG = 0, 
			TDAY_NONCASH = (SUM(NETAMT-MTOM_LOSS-MARGIN_REQ)+(MTFLEDBAL+MTFCASHCOLLATERAL+MTFNONCASHCOLLATERAL+MTFCASHEQCOLLATERAL) )
			FROM MTFTRADE.DBO.TBL_MTF_DATA 
			WHERE SAUDA_DATE BETWEEN @MARGINDATENEW AND @MARGINDATENEW + ' 23:59'  
			--WHERE SAUDA_DATE BETWEEN 'MAY 26 2020' AND 'MAY 26 2020' + ' 23:59'  
			GROUP BY SAUDA_DATE, PARTY_CODE, MTFLEDBAL, MTFCASHCOLLATERAL, MTFNONCASHCOLLATERAL, MTFCASHEQCOLLATERAL,
			CASHDEBITADJ, CASHNONCASHADJ, FONONCASHADJ, CASHLEDADJ, FOLEDBALADJ, POAADJ 
			HAVING (SUM(NETAMT-MTOM_LOSS-MARGIN_REQ)+(MTFLEDBAL+MTFCASHCOLLATERAL+MTFNONCASHCOLLATERAL+MTFCASHEQCOLLATERAL) ) > 1 

			SELECT DISTINCT PARTY_CODE INTO #MTFTDAY FROM MTFTRADE.DBO.TBL_MTF_DATA 
			WHERE SAUDA_DATE = @MARGINDATENEW

			SELECT * INTO #MTFCOLLTDAY FROM MTFTRADE.DBO.TBL_PRODUCT_HOLD_DATA H
			WHERE SAUDA_DATE = @MARGINDATENEW
			AND HOLDFLAG = 'MTFCOLL'

			SELECT PARTY_CODE, NONCASHCOLL = SUM(QTY*CL_RATE-QTY*CL_RATE*HAIRCUT/100) 
			INTO #EX_MTFTDAY
			FROM #MTFCOLLTDAY H
			WHERE SAUDA_DATE = @MARGINDATENEW
			AND HOLDFLAG = 'MTFCOLL'
			AND NOT EXISTS (SELECT PARTY_CODE FROM #MTFTDAY M
							WHERE M.PARTY_CODE = H.PARTY_CODE)
			GROUP BY PARTY_CODE 
		
			INSERT INTO #EX_MTFTDAY
			SELECT CLTCODE, NONCASHCOLL = SUM(CASE WHEN DRCR = 'C' THEN VAMT ELSE -VAMT END)
			FROM MTFTRADE.DBO.TblClientMargin T, MTFTRADE.DBO.LEDGER L, MTFTRADE.DBO.parameter P
			WHERE T.Party_Code = L.CLTCODE 
			--AND VDT BETWEEN sdtcur  AND ldtcur
			AND VDT BETWEEN sdtcur  AND  @MARGINDATENEW    +' 23:59' 
			AND @MARGINDATENEW BETWEEN sdtcur AND ldtcur 
			AND NOT EXISTS (SELECT PARTY_CODE FROM #MTFTDAY M
							WHERE M.PARTY_CODE = T.PARTY_CODE)
			GROUP BY CLTCODE 
			HAVING SUM(CASE WHEN DRCR = 'C' THEN VAMT ELSE -VAMT END) <> 0 

			INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, TDAY_CASHCOLL, TDAY_FDBG, TDAY_NONCASH)    
			SELECT MARGINDATE = @MARGINDATE, EXCHANGE = 'MTF', SEGMENT = 'MTF', PARTY_CODE, TDAY_CASHCOLL = 0, TDAY_FDBG = 0, 
			TDAY_NONCASH = SUM(NONCASHCOLL)
			FROM #EX_MTFTDAY
			GROUP BY PARTY_CODE
			HAVING SUM(NONCASHCOLL) > 1 
		END
   END
   SET @MRGCUR = CURSOR FOR             
  
    SELECT             
  
    MARGSQL = ' INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, TDAY_MARGIN, TDAY_MTM )'  
  
       + ' SELECT ''' + @MARGINDATE + ''',''' + T.EXCHANGE + ''', ''' + T.SEGMENT + ''',PARTY_CODE,MARGIN_CALL=SUM(ISNULL(' + (CASE WHEN @FORREPORTING = 'Y' THEN REPLACE(MARGINCOLUMNSNONCASH,'ADDMARGIN',0) ELSE MARGINCOLUMNSNONCASH END) + ',0)),MTM_CALL=SUM(ISNULL(' + MARGINCOLUMNSCASH  + ',0)) FROM ' +            
  
    SHARESERVER + '.'+ SHAREDB + '.DBO.' + MARGINTABLE +             
  
    ' WHERE ' + MARGINDATEFIELD + ' = (SELECT MAX(' + MARGINDATEFIELD + ') FROM ' + SHARESERVER + '.'+ SHAREDB + '.DBO.' + MARGINTABLE + ' WHERE ' + MARGINDATEFIELD + ' <= ''' +  @MARGINDATENEW +' 23:59'') AND PARTY_CODE <> ''12798'' GROUP BY PARTY_CODE'            
  
    FROM #V2_TBL_EXCHANGE_MARGIN_REPORT_PEAK T, PRADNYA..MULTICOMPANY M            
  
    WHERE T.EXCHANGE = M.EXCHANGE            
  
    AND T.SEGMENT = M.SEGMENT     
    AND M.ShareServer <> 'ANGELDEMAT'         
  
   --AND T.SEGMENT <> 'CAPITAL'   
  
    AND T.SEGMENT <> 'POA'          
   and @MARGINDATE between FROM_DATE and TO_DATE
   OPEN @MRGCUR            
  
                  
  
   FETCH NEXT FROM @MRGCUR INTO @STRSQL            
  
   WHILE @@FETCH_STATUS = 0                   
  
   BEGIN                
  
    EXEC (@STRSQL)        
  
    FETCH NEXT FROM @MRGCUR INTO @STRSQL            
  
   END             
  
   CLOSE @MRGCUR            
  
   DEALLOCATE @MRGCUR    
  
  
  
  END  
  
  
  
  IF @DAY = 2   
  
  BEGIN  
  
   INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, T1DAY_CASHCOLL, T1DAY_FDBG, T1DAY_NONCASH)  
  
   SELECT MARGINDATE=@MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE,   
  
   T1DAY_CASHCOLL = SUM(CASE WHEN COLL_TYPE = 'MARGIN' THEN MRG_FINALAMOUNT ELSE 0 END),  
  
   T1DAY_FDBG = SUM(CASE WHEN COLL_TYPE = 'BG' OR COLL_TYPE = 'FD' THEN MRG_FINALAMOUNT ELSE 0 END),  
  
   T1DAY_NONCASH = SUM(CASE WHEN COLL_TYPE = 'SEC' THEN MRG_FINALAMOUNT ELSE 0 END)   
  
   FROM  V2_TBL_COLLATERAL_MARGIN_COMBINE M  
  
   WHERE EFFDATE = @MARGINDATENEW  
  
   AND EXISTS (SELECT T.EXCHANGE FROM #V2_TBL_EXCHANGE_MARGIN_REPORT_PEAK T   
  
    WHERE T.EXCHANGE = M.EXCHANGE  
  
    AND T.SEGMENT = M.SEGMENT)  
  
   GROUP BY EXCHANGE, SEGMENT, PARTY_CODE  
  
  
	SELECT @EPNDATE = ISNULL(MAX(START_DATE),@MARGINDATENEW) FROM SETT_MST WHERE START_DATE <= @MARGINDATENEW AND SETT_TYPE='M'

	INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, T1DAY_CASHCOLL)
	SELECT TRANSDATE=@MARGINDATE, EXCHANGE = 'EPN', SEGMENT = 'EPN', PARTY_CODE, CASHBENEFIT = SUM(CASHBENEFIT)
	FROM TBL_EPN_BENEFIT
	WHERE TRANSDATE = @EPNDATE
	AND CASHBENEFIT > 0 
	GROUP BY PARTY_CODE
 
   IF CONVERT(DATETIME,@MARGINDATE) >= CONVERT(DATETIME, 'MAY 27 2020') AND CONVERT(DATETIME,@MARGINDATE) < CONVERT(DATETIME, 'SEP 16 2020')
   BEGIN
		INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, T1DAY_CASHCOLL, T1DAY_FDBG, T1DAY_NONCASH)    
		SELECT MARGINDATE = @MARGINDATE, EXCHANGE = 'MTF', SEGMENT = 'MTF', PARTY_CODE, TDAY_CASHCOLL = 0, TDAY_FDBG = 0, 
		TDAY_NONCASH = (SUM(NETAMT-MTOM_LOSS-MARGIN_REQ)+(MTFLEDBAL+MTFCASHCOLLATERAL+MTFNONCASHCOLLATERAL+MTFCASHEQCOLLATERAL) )
		FROM MTFTRADE.DBO.TBL_MTF_DATA 
		WHERE SAUDA_DATE BETWEEN @MARGINDATENEW AND @MARGINDATENEW + ' 23:59'  
		--WHERE SAUDA_DATE BETWEEN 'MAY 26 2020' AND 'MAY 26 2020' + ' 23:59'  
		GROUP BY SAUDA_DATE, PARTY_CODE, MTFLEDBAL, MTFCASHCOLLATERAL, MTFNONCASHCOLLATERAL, MTFCASHEQCOLLATERAL,
		CASHDEBITADJ, CASHNONCASHADJ, FONONCASHADJ, CASHLEDADJ, FOLEDBALADJ, POAADJ 
		HAVING (SUM(NETAMT-MTOM_LOSS-MARGIN_REQ)+(MTFLEDBAL+MTFCASHCOLLATERAL+MTFNONCASHCOLLATERAL+MTFCASHEQCOLLATERAL) ) > 1 

		
		SELECT DISTINCT PARTY_CODE INTO #MTFT1DAY FROM MTFTRADE.DBO.TBL_MTF_DATA 
		WHERE SAUDA_DATE = @MARGINDATENEW

		SELECT * INTO #MTFCOLLT1DAY FROM MTFTRADE.DBO.TBL_PRODUCT_HOLD_DATA H
		WHERE SAUDA_DATE = @MARGINDATENEW
		AND HOLDFLAG = 'MTFCOLL'

		SELECT PARTY_CODE, NONCASHCOLL = SUM(QTY*CL_RATE-QTY*CL_RATE*HAIRCUT/100) 
		INTO #EX_MTFT1DAY
		FROM #MTFCOLLT1DAY H
		WHERE SAUDA_DATE = @MARGINDATENEW
		AND HOLDFLAG = 'MTFCOLL'
		AND NOT EXISTS (SELECT PARTY_CODE FROM #MTFT1DAY M
						WHERE M.PARTY_CODE = H.PARTY_CODE)
		GROUP BY PARTY_CODE 
		
		INSERT INTO #EX_MTFT1DAY
		SELECT CLTCODE, NONCASHCOLL = SUM(CASE WHEN DRCR = 'C' THEN VAMT ELSE -VAMT END)
		FROM MTFTRADE.DBO.TblClientMargin T, MTFTRADE.DBO.LEDGER L, MTFTRADE.DBO.parameter P
		WHERE T.Party_Code = L.CLTCODE 
		--AND VDT BETWEEN sdtcur  AND ldtcur
		 AND VDT BETWEEN sdtcur  AND  @MARGINDATENEW    +' 23:59'
		AND @MARGINDATENEW BETWEEN sdtcur AND ldtcur 
		AND NOT EXISTS (SELECT PARTY_CODE FROM #MTFT1DAY M
						WHERE M.PARTY_CODE = T.PARTY_CODE)
		GROUP BY CLTCODE 
		HAVING SUM(CASE WHEN DRCR = 'C' THEN VAMT ELSE -VAMT END) <> 0 

		INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, T1DAY_CASHCOLL, T1DAY_FDBG, T1DAY_NONCASH)    
		SELECT MARGINDATE = @MARGINDATE, EXCHANGE = 'MTF', SEGMENT = 'MTF', PARTY_CODE, TDAY_CASHCOLL = 0, TDAY_FDBG = 0, 
		TDAY_NONCASH = SUM(NONCASHCOLL)
		FROM #EX_MTFT1DAY
		GROUP BY PARTY_CODE
		HAVING SUM(NONCASHCOLL) > 1 
	END

  
    
  
   SET @MRGCUR = CURSOR FOR             
  
    SELECT             
  
    MARGSQL = ' INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, T1DAY_MARGIN, T1DAY_MTM )'  
  
       + ' SELECT ''' + @MARGINDATE + ''',''' + T.EXCHANGE + ''', ''' + T.SEGMENT + ''',PARTY_CODE,MARGIN_CALL=SUM(' + MARGINCOLUMNSNONCASH  + '),MTM_CALL=SUM(' + MARGINCOLUMNSCASH  + ') FROM ' +            
  
    SHARESERVER + '.'+ SHAREDB + '.DBO.' + MARGINTABLE +             
  
    ' WHERE ' + MARGINDATEFIELD + ' = (SELECT MAX(' + MARGINDATEFIELD + ') FROM ' + SHARESERVER + '.'+ SHAREDB + '.DBO.' + MARGINTABLE + ' WHERE ' + MARGINDATEFIELD + ' <= ''' +  @MARGINDATENEW +' 23:59'') AND PARTY_CODE <> ''12798'' GROUP BY PARTY_CODE'            
  
    FROM #V2_TBL_EXCHANGE_MARGIN_REPORT_PEAK T, PRADNYA..MULTICOMPANY M            
  
    WHERE T.EXCHANGE = M.EXCHANGE            
  
    AND T.SEGMENT = M.SEGMENT     
    AND M.ShareServer <> 'ANGELDEMAT'   
             
  
  --- AND T.SEGMENT <> 'CAPITAL'   
  
    AND T.SEGMENT <> 'POA'    
   and @MARGINDATE between FROM_DATE and TO_DATE           
  
   OPEN @MRGCUR            
  
                  
  
   FETCH NEXT FROM @MRGCUR INTO @STRSQL            
  
   WHILE @@FETCH_STATUS = 0                   
  
   BEGIN                
  
    EXEC (@STRSQL)     
  
    FETCH NEXT FROM @MRGCUR INTO @STRSQL            
  
   END             
  
   CLOSE @MRGCUR            
  
   DEALLOCATE @MRGCUR    
  
  
  
  END  
  
  
  
  IF @DAY = 3   
  
  BEGIN  
  
   INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, T2DAY_CASHCOLL, T2DAY_FDBG, T2DAY_NONCASH)  
  
   SELECT MARGINDATE=@MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE,   
  
   T2DAY_CASHCOLL = SUM(CASE WHEN COLL_TYPE = 'MARGIN' THEN MRG_FINALAMOUNT ELSE 0 END),  
  
   T2DAY_FDBG = SUM(CASE WHEN COLL_TYPE = 'BG' OR COLL_TYPE = 'FD' THEN MRG_FINALAMOUNT ELSE 0 END),  
  
   T2DAY_NONCASH = SUM(CASE WHEN COLL_TYPE = 'SEC' THEN MRG_FINALAMOUNT ELSE 0 END)   
  
   FROM  V2_TBL_COLLATERAL_MARGIN_COMBINE M  
  
   WHERE EFFDATE = @MARGINDATENEW  
  
   AND EXISTS (SELECT T.EXCHANGE FROM #V2_TBL_EXCHANGE_MARGIN_REPORT_PEAK T   
  
    WHERE T.EXCHANGE = M.EXCHANGE  
  
    AND T.SEGMENT = M.SEGMENT)  
  
   GROUP BY EXCHANGE, SEGMENT, PARTY_CODE  
  
  
	SELECT @EPNDATE = ISNULL(MAX(START_DATE),@MARGINDATENEW) FROM SETT_MST WHERE START_DATE <= @MARGINDATENEW AND SETT_TYPE='M'
	  
	INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, T2DAY_CASHCOLL)
	SELECT TRANSDATE=@MARGINDATE, EXCHANGE = 'EPN', SEGMENT = 'EPN', PARTY_CODE, CASHBENEFIT = SUM(CASHBENEFIT)
	FROM TBL_EPN_BENEFIT
	WHERE TRANSDATE = @EPNDATE
	AND CASHBENEFIT > 0 
	GROUP BY PARTY_CODE
 
   IF CONVERT(DATETIME,@MARGINDATE) >= CONVERT(DATETIME, 'MAY 27 2020') AND CONVERT(DATETIME,@MARGINDATE) < CONVERT(DATETIME, 'SEP 16 2020')
   BEGIN
		INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, T2DAY_CASHCOLL, T2DAY_FDBG, T2DAY_NONCASH)    
		SELECT MARGINDATE = @MARGINDATE, EXCHANGE = 'MTF', SEGMENT = 'MTF', PARTY_CODE, TDAY_CASHCOLL = 0, TDAY_FDBG = 0, 
		TDAY_NONCASH = (SUM(NETAMT-MTOM_LOSS-MARGIN_REQ)+(MTFLEDBAL+MTFCASHCOLLATERAL+MTFNONCASHCOLLATERAL+MTFCASHEQCOLLATERAL) )
		FROM MTFTRADE.DBO.TBL_MTF_DATA 
		WHERE SAUDA_DATE BETWEEN @MARGINDATENEW AND @MARGINDATENEW + ' 23:59'  
		--WHERE SAUDA_DATE BETWEEN 'MAY 26 2020' AND 'MAY 26 2020' + ' 23:59'  
		GROUP BY SAUDA_DATE, PARTY_CODE, MTFLEDBAL, MTFCASHCOLLATERAL, MTFNONCASHCOLLATERAL, MTFCASHEQCOLLATERAL,
		CASHDEBITADJ, CASHNONCASHADJ, FONONCASHADJ, CASHLEDADJ, FOLEDBALADJ, POAADJ 
		HAVING (SUM(NETAMT-MTOM_LOSS-MARGIN_REQ)+(MTFLEDBAL+MTFCASHCOLLATERAL+MTFNONCASHCOLLATERAL+MTFCASHEQCOLLATERAL) ) > 1 

		
		SELECT DISTINCT PARTY_CODE INTO #MTFT2DAY FROM MTFTRADE.DBO.TBL_MTF_DATA 
		WHERE SAUDA_DATE = @MARGINDATENEW

		SELECT * INTO #MTFCOLLT2DAY FROM MTFTRADE.DBO.TBL_PRODUCT_HOLD_DATA H
		WHERE SAUDA_DATE = @MARGINDATENEW
		AND HOLDFLAG = 'MTFCOLL'

		SELECT PARTY_CODE, NONCASHCOLL = SUM(QTY*CL_RATE-QTY*CL_RATE*HAIRCUT/100) 
		INTO #EX_MTFT2DAY
		FROM #MTFCOLLT2DAY H
		WHERE SAUDA_DATE = @MARGINDATENEW
		AND HOLDFLAG = 'MTFCOLL'
		AND NOT EXISTS (SELECT PARTY_CODE FROM #MTFT2DAY M
						WHERE M.PARTY_CODE = H.PARTY_CODE)
		GROUP BY PARTY_CODE 
		
		INSERT INTO #EX_MTFT2DAY
		SELECT CLTCODE, NONCASHCOLL = SUM(CASE WHEN DRCR = 'C' THEN VAMT ELSE -VAMT END)
		FROM MTFTRADE.DBO.TblClientMargin T, MTFTRADE.DBO.LEDGER L, MTFTRADE.DBO.parameter P
		WHERE T.Party_Code = L.CLTCODE 
		--AND VDT BETWEEN sdtcur  AND ldtcur 
		AND VDT BETWEEN sdtcur  AND  @MARGINDATENEW    +' 23:59' 
		AND @MARGINDATENEW BETWEEN sdtcur AND ldtcur 
		AND NOT EXISTS (SELECT PARTY_CODE FROM #MTFT2DAY M
						WHERE M.PARTY_CODE = T.PARTY_CODE)
		GROUP BY CLTCODE 
		HAVING SUM(CASE WHEN DRCR = 'C' THEN VAMT ELSE -VAMT END) <> 0 

		INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, T2DAY_CASHCOLL, T2DAY_FDBG, T2DAY_NONCASH)    
		SELECT MARGINDATE = @MARGINDATE, EXCHANGE = 'MTF', SEGMENT = 'MTF', PARTY_CODE, TDAY_CASHCOLL = 0, TDAY_FDBG = 0, 
		TDAY_NONCASH = SUM(NONCASHCOLL)
		FROM #EX_MTFT2DAY
		GROUP BY PARTY_CODE
		HAVING SUM(NONCASHCOLL) > 1 
	END
  
  
   SET @MRGCUR = CURSOR FOR             
  
    SELECT             
  
    MARGSQL = ' INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, T2DAY_MARGIN, T2DAY_MTM )'  
  
       + ' SELECT ''' + @MARGINDATE + ''',''' + T.EXCHANGE + ''', ''' + T.SEGMENT + ''',PARTY_CODE,MARGIN_CALL=SUM(' + MARGINCOLUMNSNONCASH  + '),MTM_CALL=SUM(' + MARGINCOLUMNSCASH  + ') FROM ' +            
  
    SHARESERVER + '.'+ SHAREDB + '.DBO.' + MARGINTABLE +             
  
    ' WHERE ' + MARGINDATEFIELD + ' = (SELECT MAX(' + MARGINDATEFIELD + ') FROM ' + SHARESERVER + '.'+ SHAREDB + '.DBO.' + MARGINTABLE + ' WHERE ' + MARGINDATEFIELD + ' <= ''' +  @MARGINDATENEW +' 23:59'') AND PARTY_CODE <> ''12798'' GROUP BY PARTY_CODE'            
  
    FROM #V2_TBL_EXCHANGE_MARGIN_REPORT_PEAK T, PRADNYA..MULTICOMPANY M            
  
    WHERE T.EXCHANGE = M.EXCHANGE            
  
    AND T.SEGMENT = M.SEGMENT            
  
    AND M.ShareServer <> 'ANGELDEMAT'   
   --- AND T.SEGMENT <> 'CAPITAL'      
  
    AND T.SEGMENT <> 'POA'   
   and @MARGINDATE between FROM_DATE and TO_DATE         
  
   OPEN @MRGCUR            
  
                  
  
   FETCH NEXT FROM @MRGCUR INTO @STRSQL            
  
   WHILE @@FETCH_STATUS = 0                   
  
   BEGIN                
  
    EXEC (@STRSQL)   
  
      
  
    FETCH NEXT FROM @MRGCUR INTO @STRSQL            
  
   END             
  
   CLOSE @MRGCUR            
  
   DEALLOCATE @MRGCUR    
  
  
  
  END  
  
  SET @DAY = @DAY + 1  
  
  SET @CNT = @CNT + 1   
  
  
  
  DEALLOCATE @EXCHANGEWISE_CURSOR  
  
  FETCH NEXT FROM @MARGINDATECURSOR INTO @MARGINDATENEW  
  
 END  
  
 CLOSE @MARGINDATECURSOR  
  
 DEALLOCATE @MARGINDATECURSOR  
  
  
  
 IF @FORREPORTING <> 'Y'   
  
 BEGIN  
  
  DELETE FROM TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL  
  
  WHERE MARGINDATE = @MARGINDATE  
  
  AND Exchange = 'POA' AND SEGMENT = 'POA'  
  
 END  

 IF @CHECKFLAG = 0 
 BEGIN
	UPDATE T  SET TDAY_MARGIN = CONVERT(NUMERIC(18,2),TDAY_MARGIN * PEAK_PER / 100)
  FROM #V2_TBL_EXCHANGE_MARGIN_REPORT_PEAK P (Nolock),TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL T
  WHERE MARGINDATE = @MARGINDATE  
  AND T.EXCHANGE = P.EXCHANGE 
  AND T.SEGMENT = P.SEGMENT 
  AND PEAK_PER > 0 
 END
  
END  
/*
DELETE FROM TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL
WHERE MARGINDATE = @MARGINDATE AND EXCHANGE = 'EPN'

INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, TDAY_NONCASH, TDAY_CASHCOLL)
SELECT TRANSDATE, EXCHANGE = 'EPN', SEGMENT = 'EPN', PARTY_CODE, TDAY_NONCASH = SUM(MARGIN_BENEFIT), CASHBENEFIT = SUM(CASHBENEFIT)
FROM TBL_EPN_BENEFIT
WHERE TRANSDATE = @MARGINDATE
AND MARGIN_BENEFIT + CASHBENEFIT > 0 
GROUP BY TRANSDATE, PARTY_CODE
*/
 
IF CONVERT(DATETIME,@MARGINDATE) = CONVERT(DATETIME,'OCT  6 2023')

BEGIN 
    
	 SELECT CLTCODE,SUM(VAMT) AS BAL INTO #SEBIPOBAL FROM ACCOUNT.DBO.LEDGER(NOLOCK) WHERE EDT=@MARGINDATE AND VTYP=3  
	 AND NARRATION LIKE 'AMT TRANSFER QUARTERLY SETTLEMENT%' AND DRCR='D'	 
	 GROUP BY CLTCODE 
	 

 	INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, TDAY_LEDGER)
	SELECT @MARGINDATE, EXCHANGE = 'NSE', SEGMENT = 'CAPITAL', PARTY_CODE=CLTCODE, TDAY_LEDGER = SUM(BAL)
	FROM #SEBIPOBAL
	GROUP BY CLTCODE

END 
 



IF CONVERT(DATETIME,@MARGINDATE) >= CONVERT(DATETIME,'SEP 16 2020')
BEGIN
	DELETE FROM TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL
	WHERE MARGINDATE = @MARGINDATE AND EXCHANGE = 'MTF'

	CREATE TABLE #MTFDATA
	(
		PARTY_CODE	VARCHAR(10),
		MTF_AVL		NUMERIC(18,4),
		MARGIN_REQ	NUMERIC(18,4),
		COLLECTED	NUMERIC(18,4)
	)

	INSERT INTO #MTFDATA 
	EXEC PROC_MTF_CASH_MAR_ADJUST @MARGINDATE, '0', 'ZZZZZZZZZZ', 1
	
	INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL (MARGINDATE, EXCHANGE, SEGMENT, PARTY_CODE, TDAY_NONCASH, TDAY_CASHCOLL)
	SELECT @MARGINDATE, EXCHANGE = 'MTF', SEGMENT = 'MTF', PARTY_CODE, TDAY_NONCASH = SUM(COLLECTED), TDAY_CASHCOLL = 0
	FROM #MTFDATA
	WHERE COLLECTED > 0  
	GROUP BY PARTY_CODE
END

/* New Bill Addition changes added **/
		 IF CONVERT(DATETIME,@MARGINDATE) >= CONVERT(DATETIME,'FEB 10 2025')  AND  CONVERT(DATETIME,@MARGINDATE) <= CONVERT(DATETIME,'FEB 19 2025') 

		 BEGIN 

		    --SELECT * FROM SETT_MST WHERE Sec_payin =@MARGINDATE and sett_type ='M'
		    
			 SELECT DISTINCT CLTCODE  INTO #BILL -- INTO #TEMP 
			FROM ACCOUNT.DBO.LEDGER_ALL WHERE VDT=@PREVTRADEDATE AND  EDT =@MARGINDATE AND VTYP ='15' AND DRCR='D' 
			GROUP BY CLTCODE 

		CREATE INDEX #C ON #BILL(CLTCODE)
		SELECT CLTCODE,  SUM(CASE WHEN DRCR ='D' THEN VAMT ELSE VAMT*-1 END) AS VAMT ,EXCHANGE INTO #MTFBILLREVERSAL
		FROM ACCOUNT.DBO.LEDGER_ALL WHERE VDT=@PREVTRADEDATE AND  EDT =@MARGINDATE AND VTYP IN ('35','15') AND CLTCODE IN (SELECT * FROM  #BILL )
		GROUP BY CLTCODE,EXCHANGE

		UPDATE  #MTFBILLREVERSAL  SET VAMT=(CASE WHEN VAMT <0  THEN 0 ELSE VAMT END) 
 
		INSERT INTO #MTFBILLREVERSAL
		SELECT CLTCODE,  SUM(CASE WHEN DRCR ='D' THEN VAMT ELSE VAMT*-1 END) AS VAMT  ,EXCHANGE 
		FROM ACCOUNT.DBO.LEDGER_ALL WHERE VDT =@MARGINDATE AND EDT =@MARGINDATE AND VTYP =35 
		GROUP BY CLTCODE ,EXCHANGE


		UPDATE  #MTFBILLREVERSAL  SET VAMT=(CASE WHEN VAMT <0  THEN 0 ELSE VAMT END) 

		SELECT CLTCODE,  SUM(CASE WHEN DRCR ='D' THEN VAMT ELSE VAMT*-1 END) AS VAMT ,EXCHANGE INTO #JV
		FROM ACCOUNT.DBO.LEDGER_ALL WHERE VDT =@MARGINDATE  AND VTYP =6  
		GROUP BY CLTCODE ,EXCHANGE 


		INSERT INTO #MTFBILLREVERSAL
		SELECT *   FROM #JV WHERE VAMT >0 

		SELECT  CLTCODE,JV_REVERSAL   =  ( VAMT*-1),EXCHANGE   INTO #JVREVERSAL
		FROM #JV 
		WHERE   VAMT <0 

		SELECT CLTCODE,SUM(VAMT) AS VAMT INTO #FINAL1 FROM #MTFBILLREVERSAL GROUP BY CLTCODE 
 
 
 
		SELECT CLTCODE,SUM(VAMT) AS VAMT , EXCHANGE INTO #TEMPLED FROM #MTFBILLREVERSAL
		GROUP BY CLTCODE,EXCHANGE
 
		SELECT  CLTCODE,SUM(JV_REVERSAL) AS VAMT , EXCHANGE  INTO #JVLEDREV FROM #JVREVERSAL 
		GROUP BY CLTCODE,EXCHANGE

		SELECT CLTCODE, VAMT=SUM(VAMT) , EXCHANGE INTO #FINAL_REVERSAL FROM ( 
		SELECT CLTCODE, VAMT , EXCHANGE FROM #MTFBILLREVERSAL
		UNION ALL
		SELECT CLTCODE,-VAMT,EXCHANGE FROM #JVLEDREV )A  GROUP BY CLTCODE,EXCHANGE
  
		UPDATE T SET TDAY_LEDGER =TDAY_LEDGER+VAMT
		FROM #FINAL_REVERSAL F ,TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL T WHERE CLTCODE =PARTY_CODE AND F.EXCHANGE ='NSE' AND T.EXCHANGE ='NSE' AND 	SEGMENT='CAPITAL'

		UPDATE T SET TDAY_LEDGER =TDAY_LEDGER+VAMT
		FROM #FINAL_REVERSAL F ,TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL T WHERE CLTCODE =PARTY_CODE AND F.EXCHANGE ='NSX' AND T.EXCHANGE ='NSX' AND 	SEGMENT='FUTURES'

		UPDATE T SET TDAY_LEDGER =TDAY_LEDGER+VAMT
		FROM #FINAL_REVERSAL F ,TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL T WHERE CLTCODE =PARTY_CODE AND F.EXCHANGE ='MCX' AND T.EXCHANGE ='MCX' AND 	SEGMENT='FUTURES'

		UPDATE T SET TDAY_LEDGER =TDAY_LEDGER+VAMT
		FROM #FINAL_REVERSAL F ,TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL T WHERE CLTCODE =PARTY_CODE AND F.EXCHANGE ='NCX' AND T.EXCHANGE ='NCX' AND 	SEGMENT='FUTURES'

		UPDATE T SET TDAY_LEDGER =TDAY_LEDGER+VAMT
		FROM #FINAL_REVERSAL F ,TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL T WHERE CLTCODE =PARTY_CODE AND F.EXCHANGE ='NSEFO' AND T.EXCHANGE ='NSE' AND 	SEGMENT='FUTURES'

		UPDATE T SET TDAY_LEDGER =TDAY_LEDGER+VAMT
		FROM #FINAL_REVERSAL F ,TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL T WHERE CLTCODE =PARTY_CODE AND F.EXCHANGE ='NCE' AND T.EXCHANGE ='NCE' AND 	SEGMENT='FUTURES' 
 

		UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL SET TDAY_CASHCOLL ='0' WHERE  EXCHANGE  ='MTF'
		UPDATE F SET TDAY_NONCASH =0  FROM TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL F WHERE EXCHANGE  ='MTF' 
 
		--DELETE FROM TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL WHERE EXCHANGE  ='EPN' 
 
		-- INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL
		--SELECT 
		--MARGINDATE =@MARGINDATE,EXCHANGE='EPN' ,SEGMENT='EPN' ,PARTY_CODE,TDAY_LEDGER=0,TDAY_MARGIN=0,TDAY_MTM=0,TDAY_CASHCOLL=MARGIN_BENEFIT+CASHBENEFIT,
		--TDAY_FDBG=0,TDAY_NONCASH=0,TDAY_MARGIN_SHORT=0,TDAY_MTM_SHORT=0,T1DAY_LEDGER=0,T1DAY_MARGIN=0,
		--T1DAY_MTM=0,T1DAY_CASHCOLL=0,T1DAY_FDBG=0,T1DAY_NONCASH=0,T1DAY_MARGIN_SHORT=0,T1DAY_MTM_SHORT=0,T2DAY_LEDGER=0,T2DAY_MARGIN=0,
		--T2DAY_MTM=0,T2DAY_CASHCOLL=0,T2DAY_FDBG=0,T2DAY_NONCASH=0,T2DAY_MARGIN_SHORT=0,T2DAY_MTM_SHORT=0,T_MARGINAVL=0,T_MTMAVL=0
		--FROM #SERVER 


		 END 

 /*********** Changes Completed*******************/


SELECT 
	MARGINDATE,EXCHANGE,SEGMENT,PARTY_CODE,  
	TDAY_LEDGER=SUM(ISNULL(TDAY_LEDGER,0)),   
	TDAY_MARGIN=SUM(ISNULL(TDAY_MARGIN,0)),  
	TDAY_MTM=SUM(ISNULL(TDAY_MTM,0)),   
	TDAY_CASHCOLL=SUM(ISNULL(TDAY_CASHCOLL,0)),  
	TDAY_FDBG=SUM(ISNULL(TDAY_FDBG,0)),   
	TDAY_NONCASH=SUM(ISNULL(TDAY_NONCASH,0)),   
	TDAY_MARGIN_SHORT = 0, 
	TDAY_MTM_SHORT = 0,  
	T1DAY_LEDGER=SUM(ISNULL(T1DAY_LEDGER,0)),
	T1DAY_MARGIN=SUM(ISNULL(T1DAY_MARGIN,0)),
	T1DAY_MTM=SUM(ISNULL(T1DAY_MTM,0)),  
	T1DAY_CASHCOLL=SUM(ISNULL(T1DAY_CASHCOLL,0)),  
	T1DAY_FDBG=SUM(ISNULL(T1DAY_FDBG,0)),  
	T1DAY_NONCASH=SUM(ISNULL(T1DAY_NONCASH,0)),
	T1DAY_MARGIN_SHORT = 0,   
	T1DAY_MTM_SHORT = 0,  
	T2DAY_LEDGER=SUM(ISNULL(T2DAY_LEDGER,0)), 
	T2DAY_MARGIN=SUM(ISNULL(T2DAY_MARGIN,0)),  
	T2DAY_MTM=SUM(ISNULL(T2DAY_MTM,0)),  
	T2DAY_CASHCOLL=SUM(ISNULL(T2DAY_CASHCOLL,0)), 
	T2DAY_FDBG=SUM(ISNULL(T2DAY_FDBG,0)),  
	T2DAY_NONCASH =SUM(ISNULL(T2DAY_NONCASH,0)),
	T2DAY_MARGIN_SHORT = 0,   
	T2DAY_MTM_SHORT = 0,  
	T_MARGINAVL = 0,   
	T_MTMAVL = 0   
	INTO #COMBINE_REPORTING_DETAIL   
FROM TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL   
WHERE MARGINDATE = @MARGINDATE   
GROUP BY MARGINDATE,EXCHANGE,SEGMENT,PARTY_CODE  OPTION(USE HINT('ENABLE_PARALLEL_PLAN_PREFERENCE'))
  
CREATE INDEX #PARTY ON #COMBINE_REPORTING_DETAIL (PARTY_CODE,MARGINDATE,EXCHANGE,SEGMENT)  
  
DELETE T FROM TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL  T (NOLOCK) 
  
WHERE MARGINDATE = @MARGINDATE  OPTION(MAXDOP 4)
  
  
  
DELETE T FROM TBL_COMBINE_REPORTING_PEAK_TDAY  T (NOLOCK) 
  
WHERE MARGINDATE = @MARGINDATE  OPTION(MAXDOP 4)
  
  
  
INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY  
SELECT MARGINDATE,  
    EXCHANGE = (CASE WHEN @COMBINEFLAG = 1 THEN '' ELSE EXCHANGE END),  
    SEGMENT = (CASE WHEN @COMBINEFLAG = 1 THEN '' ELSE SEGMENT END),  
    PARTY_CODE,  
    TDAY_LEDGER=SUM(ISNULL(TDAY_LEDGER,0)),  
    TDAY_MARGIN=SUM(ISNULL(TDAY_MARGIN,0)),  
    TDAY_MTM=SUM(ISNULL(TDAY_MTM,0)),  
    TDAY_CASHCOLL=SUM(ISNULL(TDAY_CASHCOLL,0)),  
    TDAY_FDBG=SUM(ISNULL(TDAY_FDBG,0)),  
    TDAY_CASH=SUM(ISNULL(TDAY_LEDGER,0))+SUM(ISNULL(TDAY_CASHCOLL,0)),  
    TDAY_NONCASH=SUM(ISNULL(TDAY_NONCASH,0))+SUM(ISNULL(TDAY_FDBG,0)),  
    TDAY_MARGIN_SHORT = 0,   
       TDAY_MTM_SHORT = 0,  
    T1DAY_LEDGER=SUM(ISNULL(T1DAY_LEDGER,0)),  
    T1DAY_MARGIN=SUM(ISNULL(T1DAY_MARGIN,0)),  
    T1DAY_MTM=SUM(ISNULL(T1DAY_MTM,0)),  
    T1DAY_CASHCOLL=SUM(ISNULL(T1DAY_CASHCOLL,0)),  
    T1DAY_FDBG=SUM(ISNULL(T1DAY_FDBG,0)),  
    T1DAY_CASH=SUM(ISNULL(T1DAY_LEDGER,0))+SUM(ISNULL(T1DAY_CASHCOLL,0)),  
    T1DAY_NONCASH=SUM(ISNULL(T1DAY_NONCASH,0))+SUM(ISNULL(T1DAY_FDBG,0)),  
    T1DAY_MARGIN_SHORT = 0,   
       T1DAY_MTM_SHORT = 0,  
    T2DAY_LEDGER=SUM(ISNULL(T2DAY_LEDGER,0)),  
    T2DAY_MARGIN=SUM(ISNULL(T2DAY_MARGIN,0)),  
    T2DAY_MTM=SUM(ISNULL(T2DAY_MTM,0)),  
    T2DAY_CASHCOLL=SUM(ISNULL(T2DAY_CASHCOLL,0)),  
    T2DAY_FDBG=SUM(ISNULL(T2DAY_FDBG,0)),  
    T2DAY_CASH=SUM(ISNULL(T2DAY_LEDGER,0))+SUM(ISNULL(T2DAY_CASHCOLL,0)),  
    T2DAY_NONCASH =SUM(ISNULL(T2DAY_NONCASH,0))+SUM(ISNULL(T2DAY_FDBG,0)),  
    T2DAY_MARGIN_SHORT = 0,   
       T2DAY_MTM_SHORT = 0  
FROM #COMBINE_REPORTING_DETAIL  
WHERE MARGINDATE = @MARGINDATE  
GROUP BY MARGINDATE, (CASE WHEN @COMBINEFLAG = 1 THEN '' ELSE EXCHANGE END), (CASE WHEN @COMBINEFLAG = 1 THEN '' ELSE SEGMENT END), PARTY_CODE  
HAVING SUM(TDAY_MARGIN+TDAY_MTM) > 0   



  
INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY  
SELECT MARGINDATE,  
    EXCHANGE = (CASE WHEN @COMBINEFLAG = 1 THEN '' ELSE EXCHANGE END),  
    SEGMENT = (CASE WHEN @COMBINEFLAG = 1 THEN '' ELSE SEGMENT END),  
    PARTY_CODE,  
    TDAY_LEDGER=SUM(ISNULL(TDAY_LEDGER,0)),  
    TDAY_MARGIN=SUM(ISNULL(TDAY_MARGIN,0)),  
    TDAY_MTM=SUM(ISNULL(TDAY_MTM,0)),  
    TDAY_CASHCOLL=SUM(ISNULL(TDAY_CASHCOLL,0)),  
    TDAY_FDBG=SUM(ISNULL(TDAY_FDBG,0)),  
    TDAY_CASH=SUM(ISNULL(TDAY_LEDGER,0))+SUM(ISNULL(TDAY_CASHCOLL,0)),  
    TDAY_NONCASH=SUM(ISNULL(TDAY_NONCASH,0))+SUM(ISNULL(TDAY_FDBG,0)),  
    TDAY_MARGIN_SHORT = 0,   
       TDAY_MTM_SHORT = 0,  
    T1DAY_LEDGER=SUM(ISNULL(T1DAY_LEDGER,0)),  
    T1DAY_MARGIN=SUM(ISNULL(T1DAY_MARGIN,0)),  
    T1DAY_MTM=SUM(ISNULL(T1DAY_MTM,0)),  
    T1DAY_CASHCOLL=SUM(ISNULL(T1DAY_CASHCOLL,0)),  
    T1DAY_FDBG=SUM(ISNULL(T1DAY_FDBG,0)),  
    T1DAY_CASH=SUM(ISNULL(T1DAY_LEDGER,0))+SUM(ISNULL(T1DAY_CASHCOLL,0)),  
    T1DAY_NONCASH=SUM(ISNULL(T1DAY_NONCASH,0))+SUM(ISNULL(T1DAY_FDBG,0)),  
    T1DAY_MARGIN_SHORT = 0,   
       T1DAY_MTM_SHORT = 0,  
    T2DAY_LEDGER=SUM(ISNULL(T2DAY_LEDGER,0)),  
    T2DAY_MARGIN=SUM(ISNULL(T2DAY_MARGIN,0)),  
    T2DAY_MTM=SUM(ISNULL(T2DAY_MTM,0)),  
    T2DAY_CASHCOLL=SUM(ISNULL(T2DAY_CASHCOLL,0)),  
    T2DAY_FDBG=SUM(ISNULL(T2DAY_FDBG,0)),  
    T2DAY_CASH=SUM(ISNULL(T2DAY_LEDGER,0))+SUM(ISNULL(T2DAY_CASHCOLL,0)),  
    T2DAY_NONCASH =SUM(ISNULL(T2DAY_NONCASH,0))+SUM(ISNULL(T2DAY_FDBG,0)),  
    T2DAY_MARGIN_SHORT = 0,   
       T2DAY_MTM_SHORT = 0  
FROM #COMBINE_REPORTING_DETAIL  
WHERE MARGINDATE = @MARGINDATE  
AND Exists(SELECT PARTY_CODE FROM #COMBINE_REPORTING_DETAIL P WHERE #COMBINE_REPORTING_DETAIL.PARTY_CODE=P.PARTY_CODE  AND EXCHANGE = 'EPN' AND SEGMENT = 'EPN'   )
AND NOT Exists (SELECT PARTY_CODE FROM TBL_COMBINE_REPORTING_PEAK_TDAY T WHERE T.PARTY_CODE= #COMBINE_REPORTING_DETAIL.PARTY_CODE And  MARGINDATE = @MARGINDATE )
GROUP BY MARGINDATE, (CASE WHEN @COMBINEFLAG = 1 THEN '' ELSE EXCHANGE END), (CASE WHEN @COMBINEFLAG = 1 THEN '' ELSE SEGMENT END), PARTY_CODE  
--HAVING SUM(TDAY_MARGIN+TDAY_MTM) = 0 
--Having SUM(TDAY_MARGIN+TDAY_MTM)> 0

INSERT INTO TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL  
SELECT * FROM #COMBINE_REPORTING_DETAIL  
WHERE EXISTS (SELECT PARTY_CODE FROM TBL_COMBINE_REPORTING_PEAK_TDAY  
     WHERE TBL_COMBINE_REPORTING_PEAK_TDAY.MARGINDATE = @MARGINDATE  
     AND TBL_COMBINE_REPORTING_PEAK_TDAY.PARTY_CODE = #COMBINE_REPORTING_DETAIL.PARTY_CODE)  
	 
IF CONVERT(DATETIME,@MARGINDATE) >= CONVERT(DATETIME,'OCT  7 2021')
BEGIN
	SELECT PARTY_CODE, TDAY_MARGIN = MAX(ISNULL(TDAY_MARGIN,0)) INTO #HIGHPEAK FROM TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL
	WHERE TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL.MARGINDATE = @MARGINDATE  
	GROUP BY PARTY_CODE

	UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY SET 
	TDAY_MARGIN = P.TDAY_MARGIN
	FROM #HIGHPEAK P
	WHERE TBL_COMBINE_REPORTING_PEAK_TDAY.MARGINDATE = @MARGINDATE 
	AND TBL_COMBINE_REPORTING_PEAK_TDAY.PARTY_CODE = P.PARTY_CODE
END

--SELECT * FROM TBL_COMBINE_REPORTING_PEAK_TDAY WHERE MARGINDATE='FEB  3 2022'
--AND PARTY_CODE='B221147'

UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY SET 
TDAY_MARGIN = TBL_COMBINE_REPORTING_PEAK_TDAY.TDAY_MARGIN-OLDMARGIN
FROM #PEAK P
WHERE TBL_COMBINE_REPORTING_PEAK_TDAY.MARGINDATE = @MARGINDATE 
AND TBL_COMBINE_REPORTING_PEAK_TDAY.PARTY_CODE = P.PARTY_CODE

--SELECT * FROM TBL_COMBINE_REPORTING_PEAK_TDAY WHERE MARGINDATE='FEB  3 2022'
--AND PARTY_CODE='B221147'

UPDATE #EPNREV_P SET TDAY_PEAK = TDAY_PEAK - OLDMARGIN
FROM #PEAK P
WHERE #EPNREV_P.PARTY_CODE = P.PARTY_CODE

UPDATE #EPNREV_P SET CASHBENEFIT = (CASE WHEN CASHBENEFIT > TDAY_PEAK THEN TDAY_PEAK ELSE CASHBENEFIT END)
WHERE TDAY_PEAK > 0 

DELETE FROM #EPNREV_P
WHERE TDAY_PEAK <= 0

--SELECT * FROM TBL_COMBINE_REPORTING_PEAK_TDAY WHERE MARGINDATE='JAN 14 2022'
--AND PARTY_CODE='AJKP1007'

UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY SET TDAY_CASH = TDAY_CASH - CASHBENEFIT, TDAY_MARGIN = TDAY_MARGIN - CASHBENEFIT
FROM #EPNREV_P 
WHERE MARGINDATE = @MARGINDATE AND TBL_COMBINE_REPORTING_PEAK_TDAY.PARTY_CODE = #EPNREV_P.PARTY_CODE 
AND TDAY_PEAK > 0 

UPDATE #MTOMLOSS_FINAL SET MARGIN = TDAY_MARGIN
FROM TBL_COMBINE_REPORTING_PEAK_TDAY
WHERE TBL_COMBINE_REPORTING_PEAK_TDAY.MARGINDATE = @MARGINDATE 
AND TBL_COMBINE_REPORTING_PEAK_TDAY.PARTY_CODE = #MTOMLOSS_FINAL.PARTY_CODE

--SELECT * FROM TBL_COMBINE_REPORTING_PEAK_TDAY WHERE MARGINDATE='FEB  3 2022'
--AND PARTY_CODE='B221147'

UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY SET 
TDAY_MARGIN = TBL_COMBINE_REPORTING_PEAK_TDAY.TDAY_MARGIN-(CASE WHEN MARGIN > MTOMLOSS THEN MTOMLOSS ELSE MARGIN END) 
FROM #MTOMLOSS_FINAL P
WHERE TBL_COMBINE_REPORTING_PEAK_TDAY.MARGINDATE = @MARGINDATE 
AND TBL_COMBINE_REPORTING_PEAK_TDAY.PARTY_CODE = P.PARTY_CODE
AND MARGIN > 0 

--SELECT * FROM TBL_COMBINE_REPORTING_PEAK_TDAY WHERE MARGINDATE='FEB  3 2022'
--AND PARTY_CODE='B221147'

--SELECT * FROM TBL_COMBINE_REPORTING_PEAK_TDAY
--WHERE MARGINDATE = @MARGINDATE 
--AND PARTY_CODE='A112317'

--SELECT * FROM #EPNREV_P
--WHERE PARTY_CODE='AJKP1007'

--SELECT * FROM TBL_COMBINE_REPORTING_PEAK_TDAY WHERE MARGINDATE='JAN 14 2022'
--AND PARTY_CODE='AJKP1007'

UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL SET TDAY_MARGIN = TDAY_MARGIN - CASHBENEFIT
FROM #EPNREV_P 
WHERE MARGINDATE = @MARGINDATE AND TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL.PARTY_CODE = #EPNREV_P.PARTY_CODE 
AND EXCHANGE='NSE' AND SEGMENT = 'CAPITAL'
AND TDAY_PEAK > 0 and TDAY_MARGIN > 0 

UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY   
SET   
TDAY_MARGIN_SHORT = TDAY_CASH + TDAY_NONCASH    
WHERE MARGINDATE = @MARGINDATE   
 
UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY SET TDAY_MARGIN_SHORT = TDAY_MARGIN_SHORT - TDAY_MARGIN  
WHERE MARGINDATE  = @MARGINDATE AND TDAY_MARGIN > 0   
/*  
SELECT * FROM TBL_COMBINE_REPORTING_PEAK_TDAY  
WHERE MARGINDATE = 'JAN  7 2020'  
AND PARTY_CODE = 'A151487'  
*/  
  
UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY SET   
TDAY_MARGIN_SHORT = TDAY_MARGIN_SHORT - PRADNYA.DBO.FNMIN(TDAY_MARGIN_SHORT,ABS(PRADNYA.DBO.FNMIN((TDAY_MTM_SHORT-TDAY_MTM),0))),  
TDAY_MTM_SHORT = TDAY_MTM_SHORT + PRADNYA.DBO.FNMIN(TDAY_MARGIN_SHORT,ABS(PRADNYA.DBO.FNMIN((TDAY_MTM_SHORT-TDAY_MTM),0)))  
WHERE MARGINDATE  = @MARGINDATE AND TDAY_MTM_SHORT-TDAY_MTM < 0  
AND TDAY_MTM > 0 AND TDAY_MTM_SHORT > 0 AND TDAY_MARGIN_SHORT > 0   
/*  
SELECT * FROM TBL_COMBINE_REPORTING_PEAK_TDAY  
WHERE MARGINDATE = 'JAN  7 2020'  
AND PARTY_CODE = 'A151487'  
*/  

 IF CONVERT(DATETIME,@MARGINDATE) <= CONVERT(DATETIME,'SEP 15 2020')
 BEGIN    
	UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY SET TDAY_MARGIN = TDAY_MARGIN + ISNULL(TDAY_MARGIN_CASH,0), TDAY_MTM = TDAY_MTM + ISNULL(TDAY_MTM_CASH,0),  
	TDAY_MARGIN_SHORT = TDAY_MARGIN_SHORT - ISNULL(TDAY_MARGIN_CASH,0)--, TDAY_MTM_SHORT = TDAY_MTM_SHORT - ISNULL(TDAY_MTM_CASH,0)  
	FROM (SELECT CL_CODE=PARTY_CODE, TDAY_MARGIN_CASH = SUM(TDAY_MARGIN), TDAY_MTM_CASH = SUM(TDAY_MTM)  
	FROM #COMBINE_REPORTING_DETAIL   
	WHERE MARGINDATE  = @MARGINDATE  
	AND SEGMENT = 'CAPITAL'-- AND PARTY_CODE ='K90758'   
	GROUP BY PARTY_CODE) D  
	WHERE TBL_COMBINE_REPORTING_PEAK_TDAY.MARGINDATE  = @MARGINDATE  
	AND TBL_COMBINE_REPORTING_PEAK_TDAY.PARTY_CODE = D.CL_CODE   
  END
  ELSE
  BEGIN
    UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY SET  TDAY_MTM = TDAY_MTM + ISNULL(TDAY_MTM_CASH,0) 
	FROM (SELECT CL_CODE=PARTY_CODE, TDAY_MARGIN_CASH = SUM(TDAY_MARGIN), TDAY_MTM_CASH = SUM(TDAY_MTM)  
	FROM #COMBINE_REPORTING_DETAIL   
	WHERE MARGINDATE  = @MARGINDATE  
	AND SEGMENT = 'CAPITAL'-- AND PARTY_CODE ='K90758'   
	GROUP BY PARTY_CODE) D  
	WHERE TBL_COMBINE_REPORTING_PEAK_TDAY.MARGINDATE  = @MARGINDATE  
	AND TBL_COMBINE_REPORTING_PEAK_TDAY.PARTY_CODE = D.CL_CODE  
  END

UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY SET   
TDAY_MARGIN_SHORT = TDAY_MARGIN_SHORT - PRADNYA.DBO.FNMIN(TDAY_MARGIN_SHORT,ABS(PRADNYA.DBO.FNMIN((TDAY_MTM_SHORT-TDAY_MTM),0))),  
TDAY_MTM_SHORT = TDAY_MTM_SHORT + PRADNYA.DBO.FNMIN(TDAY_MARGIN_SHORT,ABS(PRADNYA.DBO.FNMIN((TDAY_MTM_SHORT-TDAY_MTM),0)))  
WHERE MARGINDATE  = @MARGINDATE AND TDAY_MTM_SHORT-TDAY_MTM < 0  
AND TDAY_MTM > 0 AND TDAY_MARGIN_SHORT > 0   
  
UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY SET TDAY_MTM_SHORT = TDAY_MTM_SHORT - TDAY_MTM  
WHERE MARGINDATE  = @MARGINDATE   
AND TDAY_MTM > 0 AND TDAY_MTM_SHORT - TDAY_MTM < 0   

IF (SELECT ISNULL(SUM(TDAY_MARGIN+TDAY_MTM),0) FROM TBL_COMBINE_REPORTING_PEAK_TDAY 
				WHERE MARGINDATE = @MARGINDATE) >= 0 
BEGIN  
	UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY SET TDAY_MARGIN_SHORT = 0,TDAY_MTM_SHORT=0
	WHERE MARGINDATE = @MARGINDATE AND TDAY_MARGIN_SHORT <> 0 
	AND PARTY_CODE IN (SELECT CL_CODE FROM CLIENT1 WHERE CL_TYPE = 'NRI')

	--UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY SET TDAY_MARGIN_SHORT =0 ---PRADNYA.DBO.FNMIN(ABS(TDAY_MARGIN_SHORT),ABS(T2DAY_LEDGER))
	--,TDAY_MTM_SHORT=0
	--WHERE MARGINDATE = @MARGINDATE AND TDAY_MARGIN_SHORT < 0 
	--AND PARTY_CODE IN (SELECT CL_CODE FROM CLIENT1 WHERE CL_TYPE = 'NRI') 
END 

--SELECT * FROM TBL_COMBINE_REPORTING_PEAK_TDAY WHERE MARGINDATE='JAN 14 2022'
--AND PARTY_CODE='AJKP1007'

UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY SET TDAY_MARGIN_SHORT =0
WHERE MARGINDATE = @MARGINDATE AND TDAY_MARGIN = 0 AND TDAY_MARGIN_SHORT < = 0

UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY SET TDAY_MARGIN_SHORT = -TDAY_MARGIN
WHERE MARGINDATE = @MARGINDATE AND TDAY_MARGIN > 0 AND TDAY_MARGIN_SHORT < 0
AND TDAY_MARGIN < ABS(TDAY_MARGIN_SHORT)

--SELECT * FROM TBL_COMBINE_REPORTING_PEAK_TDAY WHERE MARGINDATE='FEB  3 2022'
--AND PARTY_CODE='B221147'

UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY SET 
TDAY_MARGIN = TBL_COMBINE_REPORTING_PEAK_TDAY.TDAY_MARGIN+(CASE WHEN MARGIN > MTOMLOSS THEN MTOMLOSS ELSE MARGIN END),
TDAY_MARGIN_SHORT = (CASE WHEN TDAY_MARGIN_SHORT = 0 THEN (CASE WHEN MARGIN > MTOMLOSS THEN MTOMLOSS ELSE MARGIN END) ELSE TDAY_MARGIN_SHORT END)
FROM #MTOMLOSS_FINAL P
WHERE TBL_COMBINE_REPORTING_PEAK_TDAY.MARGINDATE = @MARGINDATE 
AND TBL_COMBINE_REPORTING_PEAK_TDAY.PARTY_CODE = P.PARTY_CODE
AND MARGIN > 0 

UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY SET 
TDAY_MARGIN = TBL_COMBINE_REPORTING_PEAK_TDAY.TDAY_MARGIN+OLDMARGIN, 
TDAY_MARGIN_SHORT = (CASE WHEN TDAY_MARGIN_SHORT = 0 THEN OLDMARGIN ELSE TDAY_MARGIN_SHORT END)
FROM #PEAK P
WHERE TBL_COMBINE_REPORTING_PEAK_TDAY.MARGINDATE = @MARGINDATE 
AND TBL_COMBINE_REPORTING_PEAK_TDAY.PARTY_CODE = P.PARTY_CODE

--SELECT * FROM TBL_COMBINE_REPORTING_PEAK_TDAY WHERE MARGINDATE='JAN 14 2022'
--AND PARTY_CODE='AJKP1007'

--SELECT * FROM TBL_COMBINE_REPORTING_PEAK_TDAY 
--WHERE MARGINDATE=@MARGINDATE
--AND PARTY_CODE='A112317'

 /* 
UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY SET TDAY_MTM_SHORT = PRADNYA.DBO.FNMAX(TDAY_MTM_SHORT, -TDAY_MTM)  
WHERE MARGINDATE  = @MARGINDATE AND TDAY_MTM_SHORT < 0  
*/  
IF @COMBINEFLAG = 0   
  
BEGIN     
  
 UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL   
  
 SET TDAY_MARGIN_SHORT = T1.TDAY_MARGIN_SHORT,  
  
  TDAY_MTM_SHORT = T1.TDAY_MTM_SHORT,  
  
  T1DAY_MARGIN_SHORT = T1.T1DAY_MARGIN_SHORT,  
  
  T1DAY_MTM_SHORT = T1.T1DAY_MTM_SHORT,  
  
  T2DAY_MARGIN_SHORT = T1.T2DAY_MARGIN_SHORT,  
  
  T2DAY_MTM_SHORT = T1.T2DAY_MTM_SHORT,  
  
  --T_MARGINAVL = (CASE WHEN T1.TDAY_MARGIN_SHORT >= 0 THEN T1.TDAY_MARGIN ELSE T1.TDAY_MARGIN_SHORT END),  
  
  --T_MTMAVL = (CASE WHEN T1.TDAY_MTM_SHORT >= 0 THEN T1.TDAY_MTM ELSE T1.TDAY_MTM_SHORT END)   
  
  T_MARGINAVL = (CASE WHEN T1.TDAY_MARGIN_SHORT >= 0 THEN T1.TDAY_MARGIN   
  
  ELSE (CASE WHEN T1.TDAY_MARGIN  > ABS(T1.TDAY_MARGIN_SHORT) THEN T1.TDAY_MARGIN_SHORT ELSE -T1.TDAY_MARGIN END) END),  
  
  T_MTMAVL = (CASE WHEN T1.TDAY_MTM_SHORT >= 0 THEN T1.TDAY_MTM   
  
  ELSE (CASE WHEN T1.TDAY_MTM  > ABS(T1.TDAY_MTM_SHORT) THEN T1.TDAY_MTM_SHORT ELSE -T1.TDAY_MTM END) END)   
  
 FROM TBL_COMBINE_REPORTING_PEAK_TDAY T1  
  
 WHERE TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL.MARGINDATE = @MARGINDATE   
  
 AND T1.MARGINDATE = @MARGINDATE  
  
 AND TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL.PARTY_CODE = T1.PARTY_CODE  
  
 AND TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL.EXCHANGE = T1.EXCHANGE  
  
 AND TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL.SEGMENT = T1.SEGMENT  
  
END  
  
ELSE  
  
BEGIN  

ALTER TABLE TBL_COMBINE_REPORTING_PEAK_TDAY REBUILD  
ALTER TABLE TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL REBUILD    
  
 UPDATE T  
  
 SET T_MARGINAVL = TDAY_MARGIN   
 FROM TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL T(NOLOCK)
  
 WHERE MARGINDATE = @MARGINDATE   
  
 AND EXISTS (SELECT PARTY_CODE FROM TBL_COMBINE_REPORTING_PEAK_TDAY  (NOLOCK) 
  
    WHERE MARGINDATE = @MARGINDATE   
  
    AND T.PARTY_CODE = TBL_COMBINE_REPORTING_PEAK_TDAY.PARTY_CODE  
  
    AND TDAY_MARGIN_SHORT >= 0)  
  
  
  
 UPDATE T  
  
 SET T_MTMAVL = TDAY_MTM  
  
  From TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL T (NOLOCK)
 WHERE MARGINDATE = @MARGINDATE   
  
 AND EXISTS (SELECT PARTY_CODE FROM TBL_COMBINE_REPORTING_PEAK_TDAY  (NOLOCK)
  
    WHERE MARGINDATE = @MARGINDATE   
  
    AND T.PARTY_CODE = TBL_COMBINE_REPORTING_PEAK_TDAY.PARTY_CODE  
  
    AND TDAY_MTM_SHORT >= 0)  
  
 SELECT distinct PARTY_CODE INTO #PARTYCNT FROM TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL  (NOLOCK)
 WHERE MARGINDATE = @MARGINDATE and EXCHANGE <> 'POA'  
 AND TDAY_MARGIN + TDAY_MTM > 0   
 GROUP BY PARTY_CODE   
 HAVING COUNT(1) = 1  
    
  CREATE CLUSTERED INDEX PARTIDX ON #PARTYCNT  
  (  
  PARTY_CODE  
  )  
  
 UPDATE T  
 SET T_MARGINAVL = T1.TDAY_MARGIN_SHORT, T_MTMAVL = T1.TDAY_MTM_SHORT  
 FROM TBL_COMBINE_REPORTING_PEAK_TDAY T1  (NOLOCK),TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL T (NOLOCK)
 WHERE T.MARGINDATE = @MARGINDATE   
 AND T1.MARGINDATE = @MARGINDATE AND T.TDAY_MARGIN + T.TDAY_MTM > 0   
 AND T.PARTY_CODE = T1.PARTY_CODE  
 AND EXISTS (SELECT PARTY_CODE FROM #PARTYCNT  (NOLOCK)
    WHERE #PARTYCNT.PARTY_CODE = T.PARTY_CODE)  

 IF CONVERT(DATETIME,@MARGINDATE) > CONVERT(DATETIME,'SEP 15 2020')
 BEGIN
	UPDATE #V2_TBL_EXCHANGE_MARGIN_REPORT_PEAK SET EXCHANGE_PRIORITY = 0 WHERE SEGMENT = 'CAPITAL'
 end
       
 SET @MRGCUR = CURSOR FOR  
 SELECT PARTY_CODE, AVAL_MARGIN=TDAY_MARGIN_SHORT FROM TBL_COMBINE_REPORTING_PEAK_TDAY  
 WHERE MARGINDATE = @MARGINDATE  AND TDAY_MARGIN_SHORT < 0   
 AND NOT EXISTS (SELECT PARTY_CODE FROM #PARTYCNT  
    WHERE #PARTYCNT.PARTY_CODE = TBL_COMBINE_REPORTING_PEAK_TDAY.PARTY_CODE)  
   ORDER BY PARTY_CODE   
  
 OPEN @MRGCUR  
 FETCH NEXT FROM @MRGCUR INTO @PARTY_CODE, @SHRT_AMT  
 WHILE @@FETCH_STATUS = 0  
 BEGIN  
  SET @EXCHANGEWISE_CURSOR = CURSOR FOR  
  SELECT PARTY_CODE, T.EXCHANGE, T.SEGMENT, TDAY_MARGIN, EXCHANGE_PRIORITY   
  FROM TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL T, #V2_TBL_EXCHANGE_MARGIN_REPORT_PEAK T1  
  WHERE MARGINDATE = @MARGINDATE  
  AND PARTY_CODE = @PARTY_CODE  
  AND T.EXCHANGE = T1.EXCHANGE   
  AND T.SEGMENT = T1.SEGMENT   
  AND TDAY_MARGIN > 0  
  ORDER BY TDAY_MARGIN DESC, EXCHANGE_PRIORITY DESC  
  
  OPEN @EXCHANGEWISE_CURSOR  
  
  FETCH NEXT FROM @EXCHANGEWISE_CURSOR INTO @PARTY_CODE, @EXCHANGE_NEW, @SEGMENT_NEW, @MARG_AMT, @EXCHANGE_PRIORITY  
  WHILE @@FETCH_STATUS = 0  
  
  BEGIN  
  
   IF @SHRT_AMT < 0   
  
   BEGIN  
  
    IF @MARG_AMT >= ABS(@SHRT_AMT)  
  
    BEGIN  
  
     UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL SET T_MARGINAVL = @SHRT_AMT  
     WHERE MARGINDATE = @MARGINDATE  
     AND PARTY_CODE = @PARTY_CODE  
     AND EXCHANGE = @EXCHANGE_NEW   
     AND SEGMENT = @SEGMENT_NEW   
     SET @SHRT_AMT = 0  
  
    END  
  
    ELSE  
  
    BEGIN  
  
     UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL SET T_MARGINAVL = -@MARG_AMT  
  
     WHERE MARGINDATE = @MARGINDATE  
  
     AND PARTY_CODE = @PARTY_CODE  
  
     AND EXCHANGE = @EXCHANGE_NEW   
  
     AND SEGMENT = @SEGMENT_NEW   
  
     SET @SHRT_AMT = @SHRT_AMT + @MARG_AMT  
  
    END  
  
   END  
  
   ELSE  
  
   BEGIN  
  
    UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL SET T_MARGINAVL = @MARG_AMT  
  
    WHERE MARGINDATE = @MARGINDATE  
  
    AND PARTY_CODE = @PARTY_CODE  
  
    AND EXCHANGE = @EXCHANGE_NEW   
  
    AND SEGMENT = @SEGMENT_NEW   
  
   END  
  
   FETCH NEXT FROM @EXCHANGEWISE_CURSOR INTO @PARTY_CODE, @EXCHANGE_NEW, @SEGMENT_NEW, @MARG_AMT, @EXCHANGE_PRIORITY  
  
  END  
  
  CLOSE @EXCHANGEWISE_CURSOR  
  
  DEALLOCATE @EXCHANGEWISE_CURSOR  
  
  FETCH NEXT FROM @MRGCUR INTO @PARTY_CODE, @SHRT_AMT  
  
 END  
  
 CLOSE @MRGCUR  
  
 DEALLOCATE @MRGCUR  
  
  
  
  
END  
  
UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL SET 
T_MARGINAVL = (CASE WHEN T_MARGINAVL >= 0 THEN T_MARGINAVL + CASHBENEFIT ELSE T_MARGINAVL END), 
TDAY_MARGIN = TDAY_MARGIN + CASHBENEFIT
FROM #EPNREV_P 
WHERE MARGINDATE  = @MARGINDATE AND EXCHANGE = 'NSE' AND SEGMENT = 'CAPITAL'
AND TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL.PARTY_CODE = #EPNREV_P.PARTY_CODE 
AND TDAY_PEAK > 0 

UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY SET TDAY_CASH = TDAY_CASH + CASHBENEFIT, 
TDAY_MARGIN = TDAY_MARGIN + CASHBENEFIT, 
TDAY_MARGIN_SHORT = (CASE WHEN TDAY_MARGIN_SHORT < 0 THEN TDAY_MARGIN_SHORT ELSE TDAY_MARGIN_SHORT + CASHBENEFIT END)
FROM #EPNREV_P 
WHERE MARGINDATE  = @MARGINDATE 
AND TBL_COMBINE_REPORTING_PEAK_TDAY.PARTY_CODE = #EPNREV_P.PARTY_CODE 
AND TDAY_PEAK > 0 

UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY SET TDAY_MARGIN_SHORT = A.TDAY_MARGIN_SHORT 
FROM ( 
SELECT  PARTY_CODE, TDAY_MARGIN_SHORT = SUM(T_MARGINAVL)
FROM TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL 
WHERE MARGINDATE = @MARGINDATE AND T_MARGINAVL < 0 
GROUP BY PARTY_CODE ) A
WHERE MARGINDATE = @MARGINDATE
AND A.PARTY_CODE = TBL_COMBINE_REPORTING_PEAK_TDAY.PARTY_CODE 

UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL SET T_MARGINAVL = -TDAY_MARGIN 
WHERE MARGINDATE = @MARGINDATE AND T_MARGINAVL < 0 AND ABS(T_MARGINAVL) > TDAY_MARGIN

UPDATE #EPNREV_P SET MARGIN_BENEFIT = #EPNREV_P.MARGIN_BENEFIT - #EPNREV_P.ORG_CASHBENEFIT
FROM #EPNPOOL_P P, #V2_TBL_EXCHANGE_MARGIN_REPORT_PEAK p1
WHERE #EPNREV_P.PARTY_CODE = P.PARTY_CODE
AND P1.EXCHANGE = 'NSE' AND P1.SEGMENT = 'CAPITAL'
	 
UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL set TDAY_NONCASH = TDAY_NONCASH + MARGIN_BENEFIT, TDAY_CASHCOLL = TDAY_CASHCOLL - MARGIN_BENEFIT 
from #EPNREV_P 
WHERE MARGINDATE  = @MARGINDATE 
AND TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL.PARTY_CODE = #EPNREV_P.PARTY_CODE 
AND EXCHANGE = 'EPN'
   
UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY set TDAY_NONCASH = TDAY_NONCASH + MARGIN_BENEFIT, TDAY_CASHCOLL = TDAY_CASHCOLL - MARGIN_BENEFIT 
from #EPNREV_P 
WHERE MARGINDATE  = @MARGINDATE 
AND TBL_COMBINE_REPORTING_PEAK_TDAY.PARTY_CODE = #EPNREV_P.PARTY_CODE 

UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL SET T_MARGINAVL = TDAY_MARGIN 
WHERE MARGINDATE = @MARGINDATE
AND TDAY_MARGIN < T_MARGINAVL
AND TDAY_MARGIN > 0 

UPDATE TBL_COMBINE_REPORTING_PEAK_TDAY SET TDAY_MARGIN_SHORT = TDAY_MARGIN 
WHERE MARGINDATE = @MARGINDATE
AND TDAY_MARGIN < TDAY_MARGIN_SHORT
AND TDAY_MARGIN_SHORT > TDAY_MARGIN
AND TDAY_MARGIN > 0 

SELECT PARTY_CODE INTO #NOPEAK FROM TBL_COMBINE_REPORTING_PEAK_TDAY
WHERE MARGINDATE = @MARGINDATE
AND TDAY_MARGIN = 0 

DELETE FROM TBL_COMBINE_REPORTING_PEAK_TDAY
WHERE MARGINDATE = @MARGINDATE
AND PARTY_CODE IN (SELECT PARTY_CODE FROM #NOPEAK) 
DELETE FROM TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL
WHERE MARGINDATE = @MARGINDATE
AND PARTY_CODE IN (SELECT PARTY_CODE FROM #NOPEAK) 

--SELECT * FROM TBL_COMBINE_REPORTING_PEAK_TDAY
--WHERE  MARGINDATE = @MARGINDATE
--AND PARTY_CODE='A112317'

DELETE FROM TBL_COMBINE_REPORTING_PEAK_DETAIL  
WHERE MARGINDATE = @MARGINDATE   
 
DELETE FROM TBL_COMBINE_REPORTING_PEAK    
WHERE MARGINDATE = @MARGINDATE  
   
INSERT INTO TBL_COMBINE_REPORTING_PEAK
SELECT * FROM  TBL_COMBINE_REPORTING_PEAK_TDAY    
WHERE MARGINDATE = @MARGINDATE  
  
INSERT INTO TBL_COMBINE_REPORTING_PEAK_DETAIL
SELECT * FROM  TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL    
WHERE MARGINDATE = @MARGINDATE  
  
TRUNCATE TABLE TBL_COMBINE_REPORTING_PEAK_TDAY

TRUNCATE TABLE TBL_COMBINE_REPORTING_PEAK_TDAY_DETAIL
 
IF CONVERT(DATETIME,@MARGINDATE) >= 'DEC 28 2023'
BEGIN
EXEC INHOUSE.DBO.PEAK_MARGIN_ANG_ALL_NEW @MARGINDATE

DELETE FROM TBL_COMBINE_REPORTING_PEAK_DETAIL  WHERE MARGINDATE = @MARGINDATE   
 
DELETE FROM TBL_COMBINE_REPORTING_PEAK    WHERE MARGINDATE = @MARGINDATE    

INSERT INTO TBL_COMBINE_REPORTING_PEAK_DETAIL
SELECT MARGINDATE,EXCHANGE,SEGMENT,PARTY_CODE,TDAY_LEDGER,TDAY_MARGIN=OLDMARGIN,TDAY_MTM,TDAY_CASHCOLL,TDAY_FDBG,TDAY_NONCASH,TDAY_MARGIN_SHORT,TDAY_MTM_SHORT,T1DAY_LEDGER,T1DAY_MARGIN,T1DAY_MTM,T1DAY_CASHCOLL,T1DAY_FDBG,T1DAY_NONCASH,T1DAY_MARGIN_SHORT,T1DAY_MTM_SHORT,T2DAY_LEDGER,T2DAY_MARGIN,T2DAY_MTM,T2DAY_CASHCOLL,T2DAY_FDBG,T2DAY_NONCASH,T2DAY_MARGIN_SHORT,T2DAY_MTM_SHORT,T_MARGINAVL,T_MTMAVL FROM INHOUSE.DBO.TBL_COMBINE_REPORTING_PEAK_DETAIL_INHOUSE WHERE MARGINDATE = @MARGINDATE      
  
INSERT INTO TBL_COMBINE_REPORTING_PEAK
SELECT MARGINDATE,EXCHANGE,SEGMENT,PARTY_CODE,TDAY_LEDGER,TDAY_MARGIN=OLDMARGIN,TDAY_MTM,TDAY_CASHCOLL,TDAY_FDBG,TDAY_CASH,TDAY_NONCASH,TDAY_MARGIN_SHORT,TDAY_MTM_SHORT,T1DAY_LEDGER,T1DAY_MARGIN,T1DAY_MTM,T1DAY_CASHCOLL,T1DAY_FDBG,T1DAY_CASH,T1DAY_NONCASH,T1DAY_MARGIN_SHORT,T1DAY_MTM_SHORT,T2DAY_LEDGER,T2DAY_MARGIN,T2DAY_MTM,T2DAY_CASHCOLL,T2DAY_FDBG,T2DAY_CASH,T2DAY_NONCASH,T2DAY_MARGIN_SHORT,T2DAY_MTM_SHORT FROM INHOUSE.DBO.TBL_COMBINE_REPORTING_PEAK_INHOUSE WHERE MARGINDATE = @MARGINDATE      
END

GO
