-- Object: PROCEDURE dbo.CLS_RECON_NONCMS_INDUSIND_PROCESS
-- Server: 10.253.33.91 | DB: ACCOUNT
--------------------------------------------------




CREATE PROCEDURE [dbo].[CLS_RECON_NONCMS_INDUSIND_PROCESS]
( @UPLOADID NVARCHAR(50) 
,@BANKCODE NVARCHAR(50)
,@FILE_PATH NVARCHAR(150)
,@UNAME NVARCHAR(50)) 
AS

BEGIN

IF(@FILE_PATH<>'')
	BEGIN
--SELECT @FILE_PATH RETURN
DECLARE @SQL NVARCHAR(MAX)
SET @SQL="SELECT * INTO XLImport FROM OPENDATASOURCE('Microsoft.Ace.OLEDB.12.0',
'Data Source="+@FILE_PATH +";Extended Properties=Excel 8.0')...[Sheet1$]"
EXEC (@SQL)
PRINT @SQL


			INSERT INTO #NONCMS_DATA 
			SELECT
			@UPLOADID
			,(SELECT BANKACCOUNT FROM RECON_BANK_FILEUPLOAD_LOGS(NOLOCK) WHERE UPLOADID=@UPLOADID) AS 'BANKACCOUNT'
			,@BANKCODE AS BANKCODE
			,F5 AS DESCRIPTION
			,'' AS LEDGERBALANCE
			,CASE WHEN ISNULL(F4 , '')='Credit' THEN 'C' ELSE 'D' END AS DRCR
			,CASE WHEN F6<>'' THEN CAST (F6 AS DECIMAL(20,2))  ELSE CAST (F7 AS DECIMAL(20,2)) END  --CAST (DBO.PIECE(FILE_DATA,',',5) AS DECIMAL(20,2)) AS AMT
			,CONVERT(DATETIME,F2,105)  AS VALUEDATE
			,CASE WHEN F5 LIKE '%/TRF TO %' THEN RTRIM(LTRIM(.DBO.PIECE(SUBSTRING (F5,CHARINDEX('/TRF TO',F5)+8,LEN(F5)),'/',1)))
			WHEN F5 LIKE '%/TRF FRM%' THEN RTRIM(LTRIM(.DBO.PIECE(SUBSTRING (F5,CHARINDEX('/TRF FRM',F5)+8,LEN(F5)),'///',1)))
			WHEN F5 LIKE '%PURCHASE DT%' THEN RTRIM(LTRIM(REPLACE(REPLACE(DBO.PIECE(F5,', ',3),'C/NO ',''),'///','')))
			--REPLACE(REPLACE(DBO.PIECE('PURCHASE DT 200117, ZR1130R, C/NO 1391762///',', ',3),'C/NO ',''),'///','')
			ELSE '' END	 AS REFERENCENO
			,'' AS CLTACCOUNT
			,'' AS CUSTOMERREFERENCE
			,'' AS TRANSACTIONDETAIL
			,'' AS VNO
			,'' AS VTYP
			,'' AS BOOKTYPE
			,0
			,'PENDING' AS 'UPLOADSTATUS'
			,(SELECT FILEUPLOAD_SDATE FROM RECON_BANK_FILEUPLOAD_LOGS(NOLOCK) WHERE UPLOADID=@UPLOADID) AS 'FILEUPLOAD_SDATE'
			,GETDATE()
			,@UNAME	
			FROM XLImport 
			WHERE F2<>''
			AND F1 NOT IN ('CustomerName','Bank Reference')
			AND F4 NOT IN('ToDate')
				
		DROP TABLE XLImport
		
	END
	IF(@FILE_PATH='')
	BEGIN
	--SELECT .dbo.piece(DBO.PIECE(FILE_DATA,'æ',4),':',2) FROM MSAJAG..FILEDATA WHERE PROGID=@UPLOADID  and DBO.PIECE(FILE_DATA,'æ',4) like '%Account No :%'

				INSERT INTO #NONCMS_DATA 
				SELECT
				@UPLOADID
				,(SELECT BANKACCOUNT FROM RECON_BANK_FILEUPLOAD_LOGS(NOLOCK) WHERE UPLOADID=@UPLOADID) AS 'BANKACCOUNT'
				,@BANKCODE AS BANKCODE
				,ISNULL(DBO.PIECE(FILE_DATA,'Æ',4) , '') AS DESCRIPTION
				,'' AS LEDGERBALANCE
				,CASE WHEN ISNULL(DBO.PIECE(FILE_DATA,'Æ',3) , '')='CREDIT' THEN 'C' ELSE 'D' END AS DRCR
				,CAST (DBO.PIECE(FILE_DATA,'Æ',5) AS DECIMAL(20,2))  AS AMT
				,CONVERT(DATETIME,CONVERT(VARCHAR(11),CONVERT(DATETIME,.DBO.CLS_REMOVE_SPACE(DBO.PIECE(FILE_DATA,'Æ',1)),103),101))
				,CASE WHEN DBO.PIECE(FILE_DATA,'æ',4) LIKE '%/TRF TO %' THEN RTRIM(LTRIM(.DBO.PIECE(SUBSTRING (DBO.PIECE(FILE_DATA,'æ',4),CHARINDEX('/TRF TO',DBO.PIECE(FILE_DATA,'æ',4))+8,LEN(DBO.PIECE(FILE_DATA,'æ',4))),'/',1)))
				WHEN DBO.PIECE(FILE_DATA,'æ',4) LIKE '%/TRF FRM%' THEN RTRIM(LTRIM(.DBO.PIECE(SUBSTRING (DBO.PIECE(FILE_DATA,'æ',4),CHARINDEX('/TRF FRM',DBO.PIECE(FILE_DATA,'æ',4))+8,LEN(DBO.PIECE(FILE_DATA,'æ',4))),'///',1)))
				WHEN DBO.PIECE(FILE_DATA,'æ',4) LIKE '%PURCHASE DT%' THEN RTRIM(LTRIM(REPLACE(REPLACE(DBO.PIECE(DBO.PIECE(FILE_DATA,'æ',4),', ',3),'C/NO ',''),'///','')))
				ELSE '' END  AS REFERENCENO
				,'' AS CLTACCOUNT
				,'' AS CUSTOMERREFERENCE
				,'' AS TRANSACTIONDETAIL
				,'' AS VNO
				,'' AS VTYP
				,'' AS BOOKTYPE
				,0
				,'PENDING' AS 'UPLOADSTATUS'
				,(SELECT FILEUPLOAD_SDATE FROM RECON_BANK_FILEUPLOAD_LOGS(NOLOCK) WHERE UPLOADID=@UPLOADID) AS 'FILEUPLOAD_SDATE'
				,GETDATE()
				,@UNAME	
				FROM RECON_BANK_FILEUPLOAD_LOGS U (NOLOCK)
				INNER JOIN MSAJAG.DBO.FILEDATA F (NOLOCK)
					ON U.UPLOADID = F.PROGID
					WHERE F.PROGID = @UPLOADID
					AND DBO.PIECE(FILE_DATA,'Æ',3) IN('DEBIT','CREDIT')
					AND DBO.PIECE(FILE_DATA,'Æ',3)<>''
					
			
	END
	/* OLD FORMAT
	BEGIN

				INSERT INTO #NONCMS_DATA 
				SELECT
				@UPLOADID
				,(SELECT BANKACCOUNT FROM RECON_BANK_FILEUPLOAD_LOGS(NOLOCK) WHERE UPLOADID=@UPLOADID) AS 'BANKACCOUNT'
				,@BANKCODE AS BANKCODE
				,ISNULL(DBO.PIECE(FILE_DATA,'æ',5) , '') AS DESCRIPTION
				,'' AS LEDGERBALANCE
				,CASE WHEN ISNULL(DBO.PIECE(FILE_DATA,'æ',4) , '')='CREDIT' THEN 'C' ELSE 'D' END AS DRCR
				,CASE WHEN DBO.PIECE(FILE_DATA,'æ',6)<>'' THEN CAST (DBO.PIECE(FILE_DATA,'æ',6) AS DECIMAL(20,2))  ELSE CAST (DBO.PIECE(FILE_DATA,'æ',7) AS DECIMAL(20,2)) END   AS AMT
				,CONVERT(DATETIME,DBO.PIECE(FILE_DATA,'æ',2),105)  AS VALUEDATE
				,CASE WHEN DBO.PIECE(FILE_DATA,'æ',5) LIKE '%/TRF TO %' THEN RTRIM(LTRIM(.DBO.PIECE(SUBSTRING (DBO.PIECE(FILE_DATA,'æ',5),CHARINDEX('/TRF TO',DBO.PIECE(FILE_DATA,'æ',5))+8,LEN(DBO.PIECE(FILE_DATA,'æ',5))),'/',1)))
			WHEN DBO.PIECE(FILE_DATA,'æ',5) LIKE '%/TRF FRM%' THEN RTRIM(LTRIM(.DBO.PIECE(SUBSTRING (DBO.PIECE(FILE_DATA,'æ',5),CHARINDEX('/TRF FRM',DBO.PIECE(FILE_DATA,'æ',5))+8,LEN(DBO.PIECE(FILE_DATA,'æ',5))),'///',1)))
			WHEN DBO.PIECE(FILE_DATA,'æ',5) LIKE '%PURCHASE DT%' THEN RTRIM(LTRIM(REPLACE(REPLACE(DBO.PIECE(DBO.PIECE(FILE_DATA,'æ',5),', ',3),'C/NO ',''),'///','')))
			--REPLACE(REPLACE(DBO.PIECE('PURCHASE DT 200117, ZR1130R, C/NO 1391762///',', ',3),'C/NO ',''),'///','')
			ELSE '' END AS REFERENCE
				,'' AS CLTACCOUNT
				,'' AS CUSTOMERREFERENCE
				,'' AS TRANSACTIONDETAIL
				,'' AS VNO
				,'' AS VTYP
				,'' AS BOOKTYPE
				,0
				,'PENDING' AS 'UPLOADSTATUS'
				,(SELECT FILEUPLOAD_SDATE FROM RECON_BANK_FILEUPLOAD_LOGS(NOLOCK) WHERE UPLOADID=@UPLOADID) AS 'FILEUPLOAD_SDATE'
				,GETDATE()
				,@UNAME	
				FROM RECON_BANK_FILEUPLOAD_LOGS U (NOLOCK)
				INNER JOIN MSAJAG.DBO.FILEDATA F (NOLOCK)
					ON U.UPLOADID = F.PROGID
					WHERE F.PROGID = @UPLOADID
					AND DBO.PIECE(FILE_DATA,'æ',4) in('Debit','Credit')
	END
	*/
END

GO
